# Don't copy these into the build context.
#
# The filename matters: Docker reads `.dockerignore` **exactly**. This file was committed as
# `.dockerignore.md` (2026-06-26) and was therefore inert for 173 commits — every build shipped
# `data/` (590 MB here), `.venv/` and `node_modules/` into the context.
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.coverage
.mypy_cache/
.ruff_cache/

# Frontend — the image serves the headless API; the Tauri GUI runs on the host.
node_modules/
apps/desktop/node_modules/
apps/desktop/src-tauri/target/
dist/

# Editor
.vscode/
.idea/

# Secrets — should always come from env at runtime
.env

# Runtime data — bind-mounted from host instead
data/

# Docker artifacts
Dockerfile
docker-compose.yml
.dockerignore

# Git
.git/
.gitignore

# Docs not needed in image.
# README.md is deliberately NOT listed: `pyproject.toml` declares `readme = "README.md"`, so
# setuptools needs the file present to build this project's own metadata during `uv sync`, and
# the Dockerfile copies it for exactly that reason. Excluding it here removes it from the build
# context and the COPY fails with "not found" before uv ever runs.
LICENSE

# OS
.DS_Store
Thumbs.db
