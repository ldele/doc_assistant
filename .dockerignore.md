# Don't copy these into the build context
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.coverage

# Editor
.vscode/
.idea/

# Secrets — should always come from env at runtime
.env

# Runtime data — bind-mounted from host instead
data/

# Docker artifacts
Dockerfile
docker-compose.yml
.dockerignore

# Git
.git/
.gitignore

# Docs not needed in image
README.md
LICENSE

# OS
.DS_Store
Thumbs.db