# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv*/
.pytest_cache/
.coverage
.mypy_cache/
.files/

# Editor
.vscode/
.idea/

# Secrets
.env

# Claude
# CLAUDE.md is COMMITTED — shared, public-safe agent instructions (a portfolio plus).
# Personal/transient notes go in CLAUDE.local.md (gitignored).
# .claude/ is gitignored EXCEPT the Tier-1 canonical-facts files (committed): CONTEXT.md +
# KNOWN_ISSUES.md. The SESSION.md baton (and everything else in .claude/) stays local. (ADR-001.)
CLAUDE.local.md
.claude/*
!.claude/CONTEXT.md
!.claude/KNOWN_ISSUES.md
# Rotated-out baton entries (cpc rotation, ADR-018 rule 11) — local-only, like the baton itself.
docs/archive/SESSION-archive-*.md
# Local-only convention gates that depend on a private tooling repo — never shared (see ADR-001).
.pre-commit-config.cpc.yaml
tools/conventions/

# Runtime data
data/cache/
data/chroma/
data/chroma_pc/
data/sources/
data/sources_manifest.yaml
data/issues/
data/library.db
data/eval.duckdb
data/eval.duckdb.wal
data/tables_debug/
data/figures/
data/wiki/
data/graph/
data/skeleton/
data/exports/

# Separate multi-domain test corpus (open-access arXiv PDFs + derived DB/vectors; RG-001
# multi-domain re-check, kept apart from the public corpus). All downloaded/derived.
data_multidomain/

# Backups
backups/

# Test artifacts
tests/eval/results_*.json

# OS
.DS_Store
Thumbs.db

# Other
core.py
HANDOFF.md

# private: personal research library — never in the public repo. The public eval suite
# (tests/eval/cases.public.yaml + corpus_manifest.yaml) stays committed; these fixtures run
# only against the local private corpus, so they are kept local. library.bib regenerates via
# `python -m scripts.export_bibtex`.
docs/library.bib
tests/eval/cases.yaml
tests/eval/eval_set.json
tests/eval/qualitative_questions.md
tests/eval/baselines/strict_baseline_2026_05_12.json
tests/eval/baselines/strict_parentchild_2026_05_12.json
tests/eval/baselines/strict_pc_topk8_2026_05_12.json

# private: interview-prep, not for the public repo
docs/STUDY_PLAN.md
Marker_Testings.ipynb
*.ipynb
docs/DEVLOG.md.bak
.chainlit_run.log
