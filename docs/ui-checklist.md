<!-- status: active · updated: 2026-07-17 · class: living -->

# UI checklist — doc_assistant (Phase 8, kept open)

Living status board for the desktop UI (`apps/desktop/`, Svelte 5 + Vite over the FastAPI/SSE
boundary). Phase 8 is an **open, iterative track** — this file is where UI features are checked off
as they ship and where new UI ideas are parked before they're specced.

Three parts:

1. **[Shipped](#1--shipped-committed)** — what's built and committed.
2. **[Open / verification debt](#2--open--verification-debt-keeps-phase-8-open)** — what keeps Phase 8 open.
3. **[Backlog](#3--backlog--candidate-ui-elements-the-iterative-pool)** — the pool to pull the next element from.
4. **[Per-feature review checklist](#4--per-feature-review-checklist-run-before-marking-any-ui-feature-done)** — the gate every new UI feature passes before it moves to *Shipped*.

Cross-refs: `docs/ROADMAP.md` (PR table, U-rows) · `docs/specs/feature-phase8-ui-upgrade.md` (design
lock + grill ledger) · `docs/specs/feature-rag-sandbox.md` · `docs/decisions/ADR-010-*` (sandbox
non-persistence) · `docs/decisions/ADR-011-*` (provider switch). When you check an item here, also
add the `docs/DEVLOG.md` entry and update the matching ROADMAP row — this file does not replace them.

---

## 1 · Shipped (committed)

Each box is checked only when the feature is **committed** (not merely staged) and passed the
[review checklist](#4--per-feature-review-checklist-run-before-marking-any-ui-feature-done).

### Chat / answer surface
- [x] **Streaming chat over POST-SSE** — token/step/result events parsed by hand (`api.ts::streamChat`). *(M2/M3 baseline)*
- [x] **U2 — right-aligned, width-capped user bubble; RAG answer stays full-width** — `Turn.svelte` CSS only (`align-self: flex-end`, `max-width: min(72%, 640px)`, tail-cue corner). Commit `7ee1b1e`. *Verified: preview-harness, both themes, mobile no-overflow.*
- [x] **U3 — citation side panel** — click inline `[n]` → slide-over with that source's chunk detail; source cards hidden inline by default. `SourcePanel.svelte` (new) + `Markdown.svelte` text-node linkifier + delegated click; `App.svelte` owns `activeCitation`. Commit `8ba1ffc`. *Verified: preview-harness, mocked SSE ($0).*
- [x] **U4 — "↻ New" conversation-reset button** — clears turns + citation panel + composer, mints a fresh `sessionId` (no backend context leak into the next question); session RAG overrides intentionally kept. `App.svelte` only, reuses `.ghost` (no new CSS). Commit `9ce5690`. *Verified 2026-07-13 (svelte-check 0/0; preview-harness light+dark, live-convo clear, disabled states).* Subsumed into U5's sidebar (↻ New moved there).
- [x] **U5 — app shell + conversation history** — left sidebar (Chat/Library switch, Library disabled) listing past chats backend-backed by `AnswerRecord.session_id`; reopen a chat read-only (degraded citation panel); `↻ New chat` in the sidebar; live chat preserved when viewing history (H2); mobile off-canvas drawer. Backend: `record_answer` `session_id` write-fix + `conversations.py` + `GET /api/conversations[/{sid}]`. Commit `9ce5690`. *Verified 2026-07-13 (pytest 863; svelte-check 0/0; preview-harness live on the real corpus — 2 turns→history, reopen read-only, dark + mobile, no overflow).* Spec: `docs/specs/feature-conversation-history.md`; contract: SPRINT-013.
- [x] **Earlier chat-UI refinement pass** — `ee8fe8d` (2026-07-09).
- [x] **Provenance card + low-confidence card** — effective-provider-aware token/`local` suffix. *(M1 baseline; made switch-truthful in `09afd0c`.)*

### Settings drawer
- [x] **Settings disclosure (read-only "Engine" section)** — discloses candidate pool, retrieval weights (now sourced from `config.BM25_WEIGHT`, no longer a hardcoded literal), parent-child, chunk sizes, each labelled with why it's locked / needs a re-ingest. Part of U1, commit `09afd0c`.
- [x] **U1 — RAG sandbox knobs** (ADR-010): `top_k` slider `[1, CANDIDATE_K]`, synthesis-mode segmented (AI/Human), multi-query toggle. Session-scoped, non-persistent, request-scoped through the whole turn — **no module-global mutation** (isolation-guard test). Provenance flags any effective value ≠ locked default. Commit `09afd0c`.
- [x] **U1 — manual System/Light/Dark theme** — `theme.ts` + re-keyed `app.css` (`data-theme` attr wins over the OS media query); `localStorage`, applied in `main.ts` before mount (no flash). Client-only, never a backend setting. Commit `09afd0c`.
- [x] **U1b — two niche sandbox knobs** (ADR-010 amendment): `epistemics_markers_enabled` toggle (contested/superseded chips), `reviewer_evidence_chars` number `[200, 6000]`. Same non-persistent, request-scoped mechanics. Commit `09afd0c`.
- [x] **U1c — live provider + model switch** (ADR-011 v1): `<select>` of configured providers (keyless provider rendered disabled with reason) + model input + Apply; takes effect next turn, no restart; reviewer follows an unpinned switch; persists like `source_dir`. Commit `09afd0c`.
- [x] **U6 — A/B-compare sandbox (retrieval diff, v1)** — a per-turn "Compare" button runs the query under the locked defaults vs the session override and shows the two retrieved source sets side-by-side (per-source diff badges `both`/`only A`/`only B` + an honest note); **$0** (retrieval only, no LLM). Pure `compare.py` (`diff_sources`/`compare_note`) + `ChatController.compare_retrieval` (no LLM, no module-global mutation) + `POST /api/compare` + `CompareCard.svelte`. Full-answer 2× compare deferred (cost-gated). Commit `c965418`. *Verified 2026-07-13 (pytest 875 +7; svelte-check 0/0; preview-harness live — `top_k=4` override → real depth diff + "only A" badges + depth note, defaults → no-op note, dark, no overflow, $0/offline); re-verified on the committed code 2026-07-13 (this session).* Spec: `docs/specs/feature-ab-compare-sandbox.md`; contract: SPRINT-015 (archived). **UX refinement (2026-07-13, user feedback, staged):** button renamed **"Test override"** and rendered **only while a retrieval-affecting override is set** (`top_k`/`use_multi_query` — no dead button in the default state; Reset hides it); card retitled "Retrieval comparison — defaults vs your override", columns "A — Locked defaults" / "B — Your override" (anchors the badge letters). *Verified live: hidden → set `top_k=4` → appears → A=10/B=4 card → Reset → gone.*
- [x] **"Point at a folder" source dir + first-run ingest banner** — `/api/settings` + `/api/ingest`. *(M4 data-home flow.)*
- [x] **Reset-to-locked-defaults** button (clears session overrides).
- [x] **S1+S2 — selective ingestion: "Manage files" section in Settings** — `SourceFile` registry + selection-scoped ingest backend (S1) + the sources panel (S2: scan/rescan, per-file status chips, exclude toggle, select-by-status, "Ingest selected (N)"). Commits `2893544` (S1) + `7224f10` (S2), 2026-07-15. *Verified live on the real corpus ($0): scan → ingested, exclude persists, select→ingest dedup-skip, both themes, mobile.* Spec: `docs/specs/feature-selective-ingestion.md`.

### Library space
- [x] **L1 — read-only chunk browser** — the Library sidebar tab (enabled) lists ingested docs; opening one shows its chunks as parent blocks, each `<details>`-expandable to its `child_index`-ordered child chunks. Read-only, no model: `library.py` (`group_children` + `get_document_chunks` over the live Chroma handle) + `GET /api/library/documents[/{id}]` + `LibraryBrowser.svelte` + a `chat|library` shell mode switch. Markers + figure thumbnails deferred to **L1b** (chunk_epistemics=0 / figures=0 on this corpus). Commit `aa288d9`. *Verified 2026-07-13 (pytest 868 +5; svelte-check 0/0; preview-harness live on the real corpus — 76 docs, parent/child render, 404, dark, no overflow, Chat↔Library preserves the live chat, $0/offline); re-verified on the committed code 2026-07-13 (this session).* Spec: `docs/specs/feature-library-browser.md`; contract: SPRINT-014 (archived). **Known blemish:** the list's `chunk_count` (SQLite registry, ingest-time) disagrees with the live Chroma parent/child counts shown in the detail header (e.g. "47 chunks" vs "23 parent blocks · 125 child chunks"; registry sums 11,965 vs 30,882 live) — two sources of truth on one screen. **Display refinement (2026-07-13, user feedback, staged):** list rows and the detail heading prefer **"Title — First Author [et al.]"** over the raw filename (filename → row tooltip / detail metaline); `authors` added to the list wire model (`DocumentSummary`/`LibraryDocumentPayload`/`types.ts`). *No visible change on this corpus yet — 0/76 docs carry metadata (all NULL); verified via canned-fetch harness (multi-author "et al.", single author, NULL fallback). The real lever is the metadata-enrichment backlog row below.*
- [x] **L4 Phase A — Library redesign: nav-tree rail + inventory grid + drill-down** — `LibraryGrid.svelte` + `lib/library.ts` collection model; Sidebar library mode = nav tree (All → Collections empty-state → Types → Added, adaptive); breadcrumb + Back; grid⇄list toggle persisted. Commit `9f597df` (2026-07-15) + layout/tile polish + sort control + active-keyword highlight in the follow-up commits. Phase B (folders) not built. Spec: `docs/specs/feature-library-redesign.md`.
- [x] **Metadata enrichment (deterministic backfill) + keyword de-noising** — wired the unwired `metadata_extractor` onto `Document` + `metadata_enrich.py` sidecar runner (`scripts/enrich_metadata.py`); titles 76/76 on the real corpus (was 0/76 — the enrichment row's premise); `VENUE_STOPWORDS` + repeated-token filter in `keywords.py`. Commit `8f31fe3` (2026-07-16). Local-LLM leftover extraction not built.
- [x] **Manual metadata editing (ADR-013)** — user-editable title/authors/year via the `DocumentMeta` override sidecar (user-entered wins, survives re-ingest; first UI write path) + reveal-in-explorer. `LibraryMetaEditor.svelte`; migration required on old boxes (`python -m doc_assistant.db.migrations`). Commit `e549254` (2026-07-16).
- [x] **Safe-delete (ADR-014)** — Library `⋯` menu → red Delete… + confirmation; source file → OS Recycle Bin (trash-first, abort-on-fail), then row+meta+chunks+figures+cache; `DELETE /api/library/documents/{id}` 200/404/409; new base dep `send2trash`. Commit `95817fc` (2026-07-16). *Live end-to-end delete (recycles a real file) still user-to-run; multi-select bulk delete deferred (needs ADR).*

---

## 2 · Open / verification debt (keeps Phase 8 open)

Built-but-not-fully-proven, or small follow-ups surfaced by the `09afd0c` review. **These are why
Phase 8 is not closed.**

- [ ] **Live-UI smoke test of the sandbox knobs on a real answer turn** — U1/U1b/U1c were preview-harness-verified at *construction only* (no answer turn, $0). Drive one real turn with a non-default `top_k` / synthesis-mode / markers toggle and confirm the `🧪 Session override` provenance note renders and the override actually changed retrieval. *(Cheap now the harness exists — see `.claude/launch.json`.)*
- [ ] **Live-UI smoke test of the provider switch end-to-end** — anthropic→ollama→anthropic was driven through the UI ($0), but confirm the *reviewer-follow* and the `🖥 Local model` usage line on a real flagged answer after a switch.
- [ ] **Live-UI smoke test of the marker chips** — KI-15 backend fix means chips now fire on ~3,334 real chunks, but nobody has confirmed the chip *renders* correctly in the live desktop UI since the fix (phase8 spec "Related backlog").
- [ ] **RG-012 Tier-2** — a cited turn on a clean/frozen box (pends a re-freeze bundling the data-home flow). *(Carried from M4.)*
- [ ] **Review finding — `reviewer_kind="llm_haiku"` is hardcoded** (`chat_controller.py:844`). When the reviewer follows a switch to Ollama, the persisted `AnswerReview` row records `reviewer_kind="llm_haiku"` while `model_name` is (correctly) the live model — a provenance-honesty blemish. Derive the kind from the resolved provider.
- [ ] **Review finding — server-side empty/whitespace model string is unvalidated** — `app_settings.set_llm_selection` validates provider + credential but accepts any `model`; a direct `llm_model=""` POST persists an empty model that `get_llm_selection()` then silently discards. Add `min_length=1` on the wire field or a non-empty `.strip()` check server-side.
- [ ] **Review finding (L1 verify, 2026-07-13) — library chunk counts have two sources of truth** — the doc list's `chunk_count` comes from the SQLite registry (ingest-time `Document.chunk_count`, sums to 11,965 across 76 docs) while the L1 detail header shows live Chroma parent/child counts (30,882 children total); one screen shows both (e.g. "47 chunks" list vs "23 parent blocks · 125 child chunks" detail). Either recompute/refresh the registry count from Chroma, or label the list count for what it is (ingest-time blocks).

---

## 3 · Backlog — candidate UI elements (the iterative pool)

Pull the next element from here. **Add your new UI ideas to this list.** Nothing here is specced
yet — moving one to *in-progress* means writing/locking its spec first (grill it), then building
against the [review checklist](#4--per-feature-review-checklist-run-before-marking-any-ui-feature-done).
Sourced from the phase8 spec's "Related backlog" table + `pr-m1`/`pr-m3` out-of-scope notes.

| # | Candidate UI element | Where tracked | Notes |
|---|---|---|---|
| [x] | **A/B compare sandbox** — run locked defaults vs. override side-by-side | ADR-010 option 4 · `docs/specs/feature-ab-compare-sandbox.md` · SPRINT-015 | **v1 committed 2026-07-13 (U6, `c965418`).** Retrieval diff only ($0, no LLM); moved to §1 above. **Still deferred:** the full-answer 2× compare (cost-gated, unverifiable without a model). |
| [ ] | **Rich marker UI** — hover a contested/superseded chip → the corroborating/contradicting docs, not just a bare chip | `pr-m1` out-of-scope (tagged PR-M3) | Currently a static chip (`SourceCard.svelte:17-21`). Backend markers already carry the data. |
| [ ] | **In-app PDF source viewer** — open the cited PDF at the page | `pr-m3` out-of-scope | Deferred at M3, never scheduled. |
| [ ] | **Styled table rendering** in the answer/source view | `pr-m3` out-of-scope | Marker tables render as text today. |
| [x] | **S2 — selective-ingestion sources panel** — status chips, select-by-status, exclude toggle, ingest-selected | `feature-selective-ingestion.md`, ROADMAP S1/S2 | **Committed 2026-07-15 (S1 `2893544` + S2 `7224f10`); moved to §1** as the Settings "Manage files" section. |
| [ ] | **In-app API-key entry (OS keychain)** — U1c v2 | ADR-011 v2 north-star | Explicitly deferred; needs the keyring decision from ADR-011's open questions. |
| [ ] | **Precise parent-child re-projection for markers** | `pr-m1` ADR-1 option 2 | Backend attribution-quality work, not UI — but gates marker-chip trustworthiness. |
| [ ] | **Conversation rename / delete / search / pin** | `feature-conversation-history.md` out-of-scope | Deferred from U5 (v1 has none). Rename needs a `title` column/sidecar; delete needs a careful "edit history?" call. |
| [ ] | **Conversation retention / prune** (parked from the history grill, H4) | `feature-conversation-history.md` ledger | v1 lists most-recent ~100, no delete/prune. Revisit when `answer_records` grows large — a maintenance increment (CLI or a settings action). |
| [ ] | **Rich / resumable chat rehydration** — claims + reviewer on reopened turns (`AnswerReview`/`AnswerClaim` joins), or resume a past chat as a live thread | `feature-conversation-history.md` Fork B | U5 reopens read-only. The joins + a resumable-session path are the natural follow-up. |
| [~] | **Library space** — a "Library" view: browse ingested docs → their chunks (read chunk text), plus in-app document management + optional chunk annotation | new (2026-07-13, user request); grilled + carved into L1/L1b/L2/L3 | **Carved 2026-07-13.** **L1 (chunk browser) built** (`feature-library-browser.md`, staged, §1 above). Remaining: **L1b** markers + figure thumbnails (reopens when sidecars populate); **L2** in-app ingestion mgmt → adopt `feature-selective-ingestion.md` (S1/S2); **L3** chunk annotation → a new Enrichment-Layer sidecar, **needs its own ADR** (first write path). Pairs with the "In-app PDF source viewer" row. |

| [x] | **Document metadata enrichment (title/authors/year backfill)** — populate the registry's empty metadata columns so the Library's title—author display lights up | new (2026-07-13, user request) | **Committed 2026-07-16 (`8f31fe3`); moved to §1.** Deterministic pass shipped (`metadata_enrich.py` sidecar + `scripts/enrich_metadata.py`, idempotent) — titles 76/76 on the real corpus. **Still open:** the local-LLM leftover-extraction stretch (cost discipline: prove on Ollama first). |
| [x] | **Manual metadata editing in the Library** — user-editable title/authors/year on a document | new (2026-07-13, user request) · **ADR-013** | **Committed 2026-07-16 (`e549254`); moved to §1.** The ADR the row demanded exists: `DocumentMeta` override sidecar (user-entered wins over extracted, survives enrichment re-runs). |
| [ ] | **Chunk editing + color-coded chunk state** — let the user edit chunk text; color-code **problematic** chunks (flag/highlight) and **modified** chunks (edited vs original) | new (2026-07-13, user request) | **Architecturally heavy — needs an ADR before any spec.** Editing chunks collides with the Enrichment-Layer non-negotiable (derived data never mutates the chunk store): an edit overlay sidecar vs in-place mutation, re-embedding + BM25 refresh on edit, what "problematic" means mechanically (extraction-health? user flag? epistemics?). Supersedes the old L3 "chunk annotation" stub in the Library-space carve row. |
| [ ] | **Epistemics in the Library (L1b reopening)** — surface contested/superseded markers (+ figures) on chunks in the Library browser | user request 2026-07-13 · the existing L1b carve | Blocked on data, not UI: `chunk_epistemics` and `figures` are both **0 rows** on this box's registry — the KI-15 code fix landed but the enrichment run hasn't been applied here. First step is running the epistemics build ($0/local) and confirming the sidecar populates; then L1b is buildable per the existing carve. |
| [x] | **Visual identity pass ("sexy pass") — COMPLETE: V1 `35b8627` · V2 `4fd772c` · V3a `181046c` · V3b `487f2df` (all committed by 2026-07-14)** (11-fork grill ledger in the session baton; spec `docs/specs/feature-visual-identity.md`; contracts `docs/archive/sprints/SPRINT-016-visual-identity-v1.md`, `docs/sprints/SPRINT-017-visual-identity-v2.md`) | new (2026-07-13, user request); grilled + V1-built same day; V2 built 2026-07-14 | **Locked:** full visual identity, phased **V1** tokens + fonts + icons → **V2** layout rhythm + header/wordmark + empty states + ~70ch reading measure → **V3** Tauri app icon + branding + polish audit (stop-early after V1 allowed). **Paper & ink**: warm ivory light / warm charcoal dark; **Spectral** serif on reading surfaces (answers, library chunks, excerpts, headings) + **Inter** sans chrome; **deep indigo** accent (no warn/ok collision); **Lucide** inline SVGs replace all chrome emoji; 2 shadow tokens. **Out:** shell topology (stays as verified); name stays `doc_assistant`. All assets local — no CDN. **V1 built + staged 2026-07-13** (frontend-only: `app.css` retheme + `Icon.svelte` + 9 components; `svelte-check` 0/0; preview-harness-verified $0/offline — palette light+dark, 4 icons/0 chrome emoji, serif seam on the real corpus, mobile no-overflow). **Fonts landed (vendored + committed):** 4 latin-subset woff2 (Spectral 400/italic/600 + Inter variable, ~115 KB) in `apps/desktop/src/assets/fonts/` + `lib/fonts.css` `@font-face` (imported in `main.ts`); all 4 faces verified loaded via `document.fonts`. **Light palette → white/ivory** (user feedback on the live app: the warm-ivory first cut read too beige; `--bg #ffffff` + whisper-ivory surfaces; dark + indigo accent + ink text unchanged). **V1 committed `35b8627`.** **V2 built + staged 2026-07-14 (SPRINT-017):** header/wordmark (serif `doc_assistant` + indigo book mark — **option A**, chosen with the user over sans-underscore/monogram), a coherent spacing + type scale (`--space-*`/`--text-*`/`--measure` tokens), restyled empty + first-run states with clickable **sample-question chips** (prefill the composer, no turn sent — chose "with chips" over a plain restyle), and a **~70ch reading measure** on `.md` prose. Shell topology stays out (fork #9). `svelte-check` 0/0; preview-harness-verified $0/offline (serif wordmark + indigo mark + 3 pill chips via computed styles; chip prefills+focuses composer; mocked-SSE $0 turn → answer prose capped at 510px = 68ch in a 790px column; dark flip clean; mobile 375px no-overflow + chips wrap; 0 console errors; screenshots timed out — structural verification, the documented box flakiness). **Name locked → `Provenote`** (coined *provenance+note*; availability-checked — GitHub/PyPI/npm free, no exact-name product; rejected Colofolio/Foliad/Marginalis/Veritome as taken). **V3 carved:** **V3a built + staged 2026-07-14 (SPRINT-018)** — the Provenote rename (wordmark treatment B `proven` ink + `ote` indigo / `index.html` + Tauri window `title` / Tauri `productName` + `identifier`→`com.provenote.desktop` / README H1 + `package.json` desc) + a cross-screen polish audit; internal Python/npm package + `doc-assistant-api` sidecar + `python -m doc_assistant.*` commands keep `doc_assistant` (module ≠ product). Built after the V2 commit (`4fd772c`) → clean two-commit rename history. `svelte-check` 0/0; preview-harness-verified $0/offline (wordmark both themes, `document.title`=Provenote, mobile no-overflow, 0 console errors). **V3b built + staged 2026-07-14 (SPRINT-019):** the **Provenote app icon** — a designed **laurel wreath encircling an open book** on a violet rounded tile (open book = reading; laurel = scholarship/provenance; shares the header mark's book motif, a richer gradient jewel), supplied as a user 1024px master + `tauri icon` regenerated the full `src-tauri/icons/*` set; installer-identity split (Provenote product ≠ `doc_assistant` code) recorded in **ADR-012**. Not preview-verifiable (OS icon) — dimension-checked (32→1024 + `.ico` 16→256) + a magnified contact sheet confirms graceful degradation (book+wreath crisp ≥48px, wreath softens by 32px, book carries recognition at 16px); no `src/`/API/behavior change. An earlier script-generated `book-open` cut was superseded by the designed icon (generator removed). |

| [ ] | **Evidence-only chat mode (no LLM at all)** — retrieval results only, zero interpretation | new (2026-07-13, user request) — **PLANNED 2026-07-17 = PR-1** in the plan file (see §3 note below) | **The "~90% exists" claim is broadly TRUE but two of its details were wrong (measured 2026-07-17):** (1) *"in Settings"* undersells it — `synthesis_mode` is already a **request-scoped per-turn override** (`chat_controller.py:759-761`, ADR-010 honoured, no global mutation), surfaced at `Settings.svelte:384-403`, scope = per-**app-session** (deliberately not conversation state, `App.svelte:477`). (2) *"renders evidence-only"* — it renders as a **concatenated markdown string** with an emoji+em-dash prefix (`chat_controller.py:1055-1058`, missed by the de-tell pass because it lives in `src/`); **`result.mode` is DEAD on the frontend** (live on the wire at `types.ts:30`, **zero** consumers) — a free field to consume. **Decided (user, 2026-07-17): force true $0 + inform, never gate.** Gate the rewrite at `:788` and force multi-query off at `:797` (which also fixes an existing raw-vs-resolved-override bug there). **Why forcing is nearly free:** human mode writes `"(human mode: evidence only)"` into history (`:1053`), so in a pure-human chat the rewrite LLM reads a **zero-information stub** — it is *structurally incapable* of resolving a pronoun. It costs money to accomplish nothing. **And the turn already lies:** `_human_result` hardcodes `UsageView(0, 0, …)` (`:1063`) while the rewrite really spends tokens — the fix makes an existing lie true. **Honest residual:** *mixed*-mode chats (AI turn 1 → human turn 2) genuinely regress. **Test trap:** `expand_query` (`pipeline.py:302`) takes **no counter**, so a `counter.total()==0` assertion **cannot** catch the multi-query leak — spy the pipeline (`FakeRAG.rewrite_calls`), not the counter. No ADR (ADR-010 already covers request-scoped knobs). |
| [ ] | **Unconstrained mode — corpus restraint off, measurements on** — let the model answer from internal memory; keep retrieval + the full integrity layer (provenance, reviewer, citation checks) running to *measure* what ungrounded answers do | new (2026-07-13, user request) — to plan (grill when picked) | Needs a spec + grill: two variants to resolve (no-retrieval pure-LLM vs retrieval-shown-but-prompt-unconstrained), and the measurement design (reviewer still grades claims against corpus evidence → a groundedness delta per turn). **Pairs naturally with the deferred full-answer A/B compare** (constrained vs unconstrained side-by-side). Paid-LLM per turn → KI-4 rules: prominent paid-mode badge, verification cost-gated (needs a model). |
| [ ] | **External literature discovery** — use the enrichment layers (epistemics, authors, keywords/concepts, citations, citation graph, concept graph) to find related papers/data beyond the corpus | new (2026-07-13, user request) — to plan; phase-level track (ROADMAP bullet added) | Scope: **legitimate open-access APIs only** — OpenAlex, Semantic Scholar, Crossref, arXiv, Unpaywall, CORE/DOAJ/EuropePMC (Sci-Hub excluded: distributes copyrighted papers without authorization). Shares lookup infra with the metadata-enrichment row (arXiv/Crossref by ID/DOI) — build that first. **Needs its own ADR:** first outbound-network feature beyond the LLM APIs on a local-first app (proxy/offline constraints, caching, rate limits, provenance of external hits). |
| [ ] | **Global CLI** — `doc-assistant ask "…"` callable from any terminal (installed on PATH), disabled by default | new (2026-07-13, user request) — **PARKED: post-review phase (user call)** | User's own hesitation on the record ("don't know if this is a good idea"). A repo-local CLI already exists; this is packaging/PATH + an enable flag. Thin shell over `ChatController` per the house pattern — cheap once the API surface stabilizes. Revisit after the review-phase debts (§2) clear. |
| [ ] | **MCP server** — expose the RAG as MCP tools (local stdio: retrieve/ask/library lookups) so Claude & other agents can query the corpus | new (2026-07-13, user request) — **PARKED: post-review phase (user call)** | Natural fit: a retrieval-only tool is $0 (pairs with evidence-only mode); an answer tool inherits the provider/cost rules. Post-review because the tool contract should freeze after the API surface stops moving. Same thin-shell rule as the CLI. |

| [x] | **Faceted keyword filtering → two-pane overlay** — pick keywords in an on-demand overlay (searchable list + live matching-docs preview); only the *selected* keywords stay inline; **AND** semantics; grey-out unavailable; live counts | new (2026-07-16, user feedback) · **✅ SHIPPED — commit `ce8b112`** | **v1 built 2026-07-16 (frontend-only, $0, staged):** pure `facetFilter`/`keywordFacets` in `lib/library.ts` (verified live — 76→22 narrow, 26 chips greyed, AND, Clear); retired the `{kind:'keyword'}` collection + removed the Sidebar Keywords group. **Redesign (grilled 2026-07-16, `grill-me`):** the always-on inline bar doesn't scale, so keyword picking moves into a **two-pane overlay** (reuse `LibraryMetaEditor` modal shell) — searchable keyword list (left, Zotero-style) + live document preview (right); live commit, no Apply; inline strip = selected keywords + trigger. Spec + ledger: `docs/specs/feature-keyword-filter-overlay.md`. The pure filter logic carried over unchanged. Folded v1 + redesign into commit `ce8b112` (2026-07-16). |
| [ ] | **Tag families / keyword normalization** — one canonical tag with many surface forms (LLM/LLMs, connectome≈connectomics), user-editable; a deterministic **detection** pass (morphological + `bge` embedding) proposes merges; an *optional, gated* LLM confirm pass (prove on Ollama first) | new (2026-07-16, user feedback) · **PR-1 ✅ SHIPPED `0c3b0d4` · PR-2 ✅ SHIPPED `0af43db`** (2026-07-17) — ADR-015 + `feature-tag-families.md`. **Row stays open: PR-2.5 (hardening) + PR-2.6 (family-aware tiles) SCOPED 2026-07-17** from a post-commit review — 6 verified defects (D1–D6, repros in the spec), all in the **write paths**; the read path (facet math, union-find, thin shell, default path) reviewed clean and the feature verified working live. **D1/D2 corrupt data beyond the feature** (duplicate `Concept` labels break `promote_keyword` repo-wide; rename silently drops the family's own canonical) and sit on the natural Detect→Accept→**Rename** flow. PR-3 (LLM) still parked | *Takes advantage of* `Concept`/`ConceptAlias` (family = a `Concept` + alias rows whose aliases are `Keyword` names) — **not** the concept-graph UI (graph/epistemics own a later track over the same rows). Filter = **keyword-name grouping** (union of `document_keywords`); curation in a dedicated **"Manage keywords" view**; overlay renders a family as an **atomic entry** (canonical + N forms). **PR-1 built + verified live** (76-doc corpus, 26 pre-existing curated concepts incl. `Large language model`←`llm`/`llms` collapsing correctly): 6 new `/api/library/keyword-families` routes, `LibraryManageKeywords.svelte`, family-aware `keywordsOf` accessor on `facetFilter`/`keywordFacets`. **PR-2 built + verified live**: `keyword_families.py` pure core (Tier 1 morphological stem-match, Tier 2 bge cosine clustering via union-find + edit-distance as a supporting signal), `POST /api/library/keyword-families/detect` (reuses the loaded embedder, no new model load), `scripts/detect_keyword_families.py` (report-only, no `--apply`), a "Detect proposals" section in the Manage view (Accept/Dismiss per proposal). Live run on the real corpus found a genuine proposal (`pvpo`≈`avpv pvpo`, 0.77 conf) via both the CLI and the app. Carve: ~~PR-1 end-to-end manual~~ → ~~PR-2 detection~~ → **PR-3** LLM (parked). **Also carries "favorite keywords"** — a promoted `Concept` = a de facto pinned/favorite (deferred from the keyword-overlay grill). |
| [ ] | **Missing-source handling + library-only delete** — badge documents whose source file was deleted outside safe-delete (derive live via `resolve_source_path`, like `SourceFile`'s `missing` status — no new column); add a second delete mode **"Remove from library only (keep the file on disk)"** (the inverse of ADR-014's trash-first delete); keep-chunks-but-flag-not-re-ingestable | new (2026-07-16, user feedback) · **extends ADR-014** · **PLANNED 2026-07-17 → SPLIT into PR-3 (fix `resolve_source_path`) / PR-4 (badge) / PR-5 (library-only delete)** — see the plan file (§3 note below) | **Measured 2026-07-17:** **0/76 docs are missing** → the badge has **nothing to show live**; verify by *synthesizing* (rename a source file, check, rename back) + canned-fetch, per the "0/76 metadata" precedent. Live existence check for all 76 = **1.0 ms** (4 call sites, never per-render) → **derive live is sound**. **`resolve_source_path` has 3 real bugs and must be fixed FIRST (PR-3) — the badge is wrong without it:** (a) fallback uses `config.DOCS_PATH`, not `app_settings.get_source_dir()` → **false "missing"** on a custom source dir; (b) fallback is **flat**, not recursive; (c) `Path.exists()` swallows `OSError` → permission-denied/locked/offline-UNC is **indistinguishable from deleted** → return type must widen to `tuple[Path|None, SourceState]` (PRESENT/MISSING/UNREADABLE). **Do NOT reuse `scan_sources`** for the badge — it **WRITES** (upserts rows, refreshes `last_seen`); a read-only list endpoint must not mutate the registry. **Badge = a dot, not a word-chip** — `tilehead` has no reserved height and no wrap control, so a "MISSING" chip breaks the uniform fixed-footprint tile height (reuse `.editmark` geometry + `Sources.svelte:326-329`'s existing `.chip.c-missing` colour language). **⚠ The substance of library-only delete is NOT the trash-skip — it's `SourceFile.excluded`.** `delete_document` never touches `SourceFile`; without exclusion the next `just ingest` **re-adds the doc** (the hazard ADR-014's own Context `:16-18` names) — *a bug with a button*. Seam exists: `registry.set_source_meta(session, rel_path, excluded=True)` (`ingest/registry.py:246-262`); **exclude BEFORE deleting the row** (ADR-014's Decision B is abort-on-fail-first). **Restore already exists + is discoverable:** `Sources.svelte:78-80` `toggleExclude` → `main.py:698-704` — *that mechanism is the whole reason ADR-014's rejected A1 can be revived as a second mode.* Residual: `--files` **overrides** `excluded` (must be in the dialog copy). **`is_archived` is a TRAP, not a shortcut** — ~12 read paths filter it and **nothing ever sets it**, but **the retrieval pipeline has NO `is_archived` filter and Chroma chunks carry only `doc_hash`**, so an "archived" doc would **vanish from the Library while its chunks kept being retrieved and cited**. **UI: a checkbox in the existing dialog, NOT a 4th menu item** (`openMenu` hardcodes geometry for 3 items, `LibraryGrid.svelte:68-69`; two adjacent red Delete items is a footgun). Prereq: `api.ts:118-124` discards the error `detail` → reuse the existing `errorDetail()` helper (`api.ts:358-372`). `delete_document` today always trashes the source and aborts if it can't (`library.py:316`). An orphaned doc stays fully queryable but nothing surfaces it. **Needs an ADR amendment** (a second delete contract). |
| [ ] | **Extended document metadata + web autocomplete** — surface the stored **DOI**; add `journal / url / abstract / article_type` (research/review/…); extend the `DocumentMeta` override sidecar to match; **Calibre-style web autocomplete** (fetch metadata by DOI/title from Crossref / OpenAlex / arXiv) | new (2026-07-16, user feedback) | **SPLIT 2026-07-17 (user) into PR-2 (surface stored DOI) / PR-6 (ADR-016) / PR-7 (fields+editor) / PR-8 (Crossref runner)** — see the plan file (§3 note below). **Measured 2026-07-17, and it corrects two claims in this very row:** (1) **"first outbound network call beyond the LLM APIs" is FALSE** — `sources_manifest.py:278-285` `_http_get` **already** does `urllib.request.urlopen` (scheme guard + UA + timeout + `# nosec B310` + checksum verify), as does `scripts/download_corpus.py:72-73`. The **defensible** claim is *"first outbound call from the API **serving path**"* (`apps/api/` has zero httpx/requests/urllib imports). **`_http_get` is a ready-made precedent to COPY, not a novelty to argue against.** (2) **`article_type` is NOT text-extractable here — measured 0/76.** (An earlier claim that `_SKIP_HEADINGS` "already parses the signal and throws it away" was wrong: `_is_skippable_heading` (`metadata_extractor.py:125-138`) returns a **bool skip predicate**, and `_SKIP_HEADINGS` mixes types ("research article") with **section names** ("abstract","methods","main") — it never fires as a taxonomy.) **Live corpus:** `doi` **25/76** (stored, indexed, and **100% invisible** — `grep -r doi apps/` → **zero**; `get_document_details` is called only as a 404 existence check at `main.py:454,464` with its return **discarded**), of which **2 are `type: component`** (a figure/supplement DOI — wrong for the paper; an argument **for** surfacing). `notes` **0/76 with no writer** → **don't surface it read-only**. `journal`/`url`/`abstract` columns **don't exist**. **Local-text yield is hopeless** (journal 5/76 · url 1/76 · abstract 3/76 · article_type 0/76) → **Crossref beats it on all four** (of the 25 DOIs: URL 25 · type 25 · container-title 21 · abstract 12) → **ship journal/url/article_type, defer abstract**. **Migration is ~6 appends to `db/migrations.py:25-34` `_ADDITIVE_COLUMNS`, not a new table** — the exact precedent is `("conversation_meta","title_override","TEXT",None)` at `:33`; the API does **not** `create_all` on startup, so the migration must be run by hand. **Transport DECIDED: stdlib `urllib` behind a named module-level seam, not httpx** — see the KI-10 addendum (2026-07-17); the **cpc-test-api-check gate** (`test_api_check.py:26`) trips on the literal string `urllib.request` in any test file, which is *correct pressure*: the seam must be monkeypatchable by name (precedent: `tests/integration/test_sync_sources.py`). **Do not use the `cpc: allow-live-api` pragma.** Still needs its own **ADR-016** (local-first degrade-don't-block, caching, provenance, rate limits — Crossref's polite pool wants a `mailto:` UA, **env var, never hardcoded**); shares lookup infra with the parked "External literature discovery" row. When `article_type`/`year`/`journal` land as **filter dimensions**, they reopen the parked "general Filters hub" question (`docs/specs/feature-keyword-filter-overlay.md` Parked). |

| [ ] | **Family-aware grid tiles** — render a tag family as its **atomic canonical chip** on Library tiles (`Connectome`, not `connectomes`+`connectome`+`connectomics`), matching the overlay's atomic-entry rule (ADR-015 T6) | new (2026-07-17, post-commit review of `0c3b0d4`/`0af43db`) — **= PR-2.6 in `feature-tag-families.md`** | **Carries defect D6 (verified live):** selecting a family highlights **nothing** on tiles — `LibraryGrid.svelte:129` matches raw `d.keywords` against `activeKeywords`, which holds the *canonical*; measured **0/25 chips** highlighted for a family vs **19** for control keyword `pretrained`. `orderedKeywords` (`:47-51`) breaks the same way, so the matching form can hide behind `+N`. Fixing display + highlight together (same file, same root cause — `LibraryGrid` never learned about families). Beyond the fix: stops spending the tile's scarce `CHIP_CAP` budget on duplicate forms of one concept, and ends the casing disagreement with the filter (tile `imagenet` vs overlay `ImageNet` — the vocabulary already supplies the display name). Frontend-only, `$0`, reuses the shipped pure `familyCanonicalOf`/`keywordsOf` helper — presentation only, no new data path, no ADR |

| [ ] | **Manage view at scale** — the Manage-keywords view + filter overlay hold up at ~100 families: move the "Manage keywords…" trigger out of the scroll region, autocomplete the "New family" canonical against existing families (navigate, don't duplicate), searchable pickers instead of flat pools, and **demote the 1-doc long tail** behind a "Show rare (N)" toggle | new (2026-07-17, user feedback on the live view) — **= PR-2.7 in `feature-tag-families.md`** | **Measured on the live corpus:** 60 keywords surface, **30 (exactly 50%) sit on a single document**; `FAMILIES (26)` shows only ~6 real families, the rest 0-member concepts inherited from concept-graph seeding (several with 0 docs, e.g. `BERT`). **Principle: a facet exists to partition a set — a 1-doc facet doesn't partition** (selecting it yields one doc, which search does better), so rare keywords have near-zero *filtering* value whether they're junk or gold; the threshold sweeps up the ugly strings **without classifying them**. **Demote, don't delete** — the tail is mostly *real* specialist vocabulary (`va1v`/`dl5`/`osns` = Drosophila glomeruli + neuron classes; `avpv`/`pvpo`/`mpoa` = hypothalamic nuclei; `rabv` = rabies tracing), and the overlay's existing search box is the escape hatch. **The canonical-autocomplete is also defect D3's fix** — PR-2.5 owns the `library.py` invariant, PR-2.7 owns the control that stops the user reaching it; don't build it twice. Frontend-only, `$0`, no ADR |
| [ ] | **Keyword truncation fix (extractor)** — `_TOKEN_RE` splits on `.` and `/`, mangling real domain terms: **`16p11.2` → `16p11`** (autism CNV) and **`C57BL/6` → `c57bl`** (mouse strain, 7 docs) | new (2026-07-17, traced from user feedback) — **NOT UI**: `src/doc_assistant/keywords.py` | Found while tracing the "wtf is 102ff/16p11" question to source docs. `_TOKEN_RE = [a-z0-9]+(?:[-+][a-z0-9]+)*` (`keywords.py:36`) allows `-`/`+` as internal joiners but not `.`/`/`, so dotted/slashed identifiers are cut at the separator. **Touches the ingest path, not the UI** — a fix means re-running keyword extraction over the corpus (76 docs) and re-checking the de-noising interaction (`VENUE_STOPWORDS` + repeated-token filter). Needs its own carve + a before/after keyword diff on the real corpus; **note the casefold/ASCII invariant** that the tag-families review relies on (`Keyword.name` is always ASCII-lowercase, which is why SQLite `lower()` == Python `.casefold()` there) — widening the token class must not break it. Deferred behind PR-2.5/2.6/2.7 |
| [ ] | **Keyword suppression (hide true artifacts)** — a user-editable "hide this keyword" list for genuine garbage, so it leaves the facet list permanently rather than only being demoted | new (2026-07-17, user request) — **needs a design decision before a spec** | Distinct from PR-2.7's *demotion* (which is display-only + reversible-by-search) and from tag families (*synonym collapse*). Targets the real artifacts: `mathrm` (LaTeX `\mathrm` leak), `professium`, `outflux` — **all three from one 1952 scanned Hodgkin-Huxley paper** — plus `102ff` ("p. 102ff" citation artifact), `fne-tune` (OCR of "fine-tune"), `neurosc`. **Open question = where suppression lives:** a **sidecar table** (user-override precedent: `DocumentMeta`/ADR-013) is the likely shape — it's a *user override*, not derived data, so the Enrichment-Layer Pattern doesn't govern; a code-level stoplist (`VENUE_STOPWORDS` precedent) is wrong for user-editable data. **Rejected already: modelling it as a "hidden" family** — abuses ADR-015 (a family means *synonym collapse*; overloading it corrupts every consumer of those rows). New table + migration + a UI write surface → **wants an ADR or at least a spec section**; may pair with the extractor fix above (fix what you can, suppress what you can't) |

<!-- Add new UI ideas below this line as `| [ ] | <element> | <where> | <notes> |` -->

> **§3 note — the 2026-07-17 plan file (LOCAL, outside the repo).** Evidence-only · missing-source +
> library-only delete · extended metadata were explored and planned in depth on 2026-07-17 and carved into
> **8 PRs** (PR-1 evidence-only · PR-2 DOI · PR-3 `resolve_source_path` fix · PR-4 badge · PR-5 library-only
> delete · PR-6 ADR-016 · PR-7 fields+editor · PR-8 Crossref runner), sequenced **after** tag-families
> PR-2.5/2.6/2.7. Per-PR files, seams to reuse, decisions, tests, verification and risks live in
> `C:\Users\LDELEZ\.claude\plans\perfect-let-s-plan-some-tidy-tulip.md` — **Claude Code's local scratch, NOT
> committed**. The load-bearing measurements and every corrected claim are duplicated into the three rows
> above and into `docs/DEVLOG.md` (2026-07-17) so nothing is lost if that file disappears. **When each PR is
> picked up, write its `docs/specs/` contract from the plan first** (the repo's spec-at-build-time pattern).

---

## 4 · Per-feature review checklist (run before marking any UI feature "done")

The gate for every new UI element. Derived from this repo's non-negotiables (`.claude/CONTEXT.md`
§Non-negotiable rules), ADR-010/011, and the DoD blocks in `docs/specs/feature-phase8-ui-upgrade.md`.
Copy this block per feature; check every applicable line before moving it to *Shipped*.

### Architecture & conventions
- [ ] **Thin shell** — no business logic in `apps/`; all logic in `src/doc_assistant/`. UI → library, never the reverse. (CONTEXT rule 3.)
- [ ] **Request-scoped, no module-global mutation** — any per-turn/session knob is threaded as an explicit parameter (a frozen dataclass / wire model), never assigned to a `config.*` or other module global. Concurrent turns on the shared `ChatController` singleton cannot leak into each other. (ADR-010; there's an isolation-guard test — extend it to any new field.)
- [ ] **Effective-value truthfulness** — anything provider/model-dependent reads the *effective* value (`self.rag.provider`/`.model`, `app_settings.effective_llm()`), never the import-time `LLM_PROVIDER`/`LLM_MODEL` constants (they go stale after a live switch). (ADR-011.)
- [ ] **No locked-setting change** without an eval-harness experiment + baseline (`--repeat`, beat control). A sandbox *override* is fine; changing a *default* is not a UI PR. (CONTEXT §Locked settings.)
- [ ] **Persistence boundary** — retrieval-quality-governed knobs are non-persistent (ADR-010). Only cosmetic prefs (theme) or per-install choices (source dir, provider selection) persist, and those go through `app_settings`/`localStorage`, never `.env`/`config.py`.
- [ ] **Exceptions chain** (`raise X from e`); user-facing messages translated at the UI/API boundary; a sidecar failure (markers, provenance) never breaks the turn.
- [ ] **Secrets** — no key in code or client; keys stay in `.env`. (CONTEXT rule 2.)

### Correctness & data integrity
- [ ] Default path (feature off / `overrides=None`) is **byte-identical** to before the feature.
- [ ] Bounds enforced at the validation boundary (pydantic wire model): out-of-range → 422, never a silent clamp. Note if a non-HTTP caller could bypass them.
- [ ] Any value persisted or recorded is **honest** — labels match the actual model/provider/kind that ran (see the `reviewer_kind` finding).
- [ ] Empty/whitespace/malformed input rejected server-side, not only guarded in the client.

### Frontend
- [ ] `svelte-check` — 0 errors.
- [ ] **Both themes** — light *and* dark styled; a new color reuses existing CSS vars (no new palette). Manual theme still wins over the OS media query.
- [ ] **Responsive** — no horizontal body overflow at mobile width; wide content scrolls in its own container.
- [ ] **Accessibility** — interactive controls have roles/labels (`role="radiogroup"`/`radio`, `aria-checked`, `aria-live` for async status); reduced-motion respected for transitions.
- [ ] TypeScript wire types mirror the backend model (`types.ts` ↔ `apps/api/models.py`).

### Tests & verification
- [ ] Unit + integration tests for the new behavior (incl. the no-global-mutation / isolation case where applicable); full gate green (`ruff` / `ruff format` / `mypy --strict src` / `bandit` 0 HIGH·MED / `pytest`).
- [ ] **Preview-harness verified** — at minimum construction-only ($0). For anything touching a real turn, a **live-UI smoke test** on a real answer (route through local Ollama to keep it $0 — force `--provider ollama`, then restore `.env`; watch the Anthropic credit-leak, KI/CONTEXT §Provider config).
- [ ] No live paid API calls in tests (cpc §13, gate-enforced).

### Docs
- [ ] One `docs/DEVLOG.md` entry (What / Why / Rejected / Opens).
- [ ] ROADMAP U-row updated (built → committed `<sha>`).
- [ ] This checklist updated: box checked in §1, item removed from §2/§3.
- [ ] A non-obvious design choice → an ADR in `docs/decisions/`; a stress-tested spec → `docs/specs/` (design-locked via a grill pass).
