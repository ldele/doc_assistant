<!-- status: active · updated: 2026-07-21 (compacted: closed [x] items collapsed to one-liners + links; open [ ]/[~] rows kept in full; detail in linked specs/ADRs/DEVLOG) · class: living -->

# UI checklist — doc_assistant (Phase 8, kept open)

Living status board for the desktop UI (`apps/desktop/`, Svelte 5 + Vite over the FastAPI/SSE
boundary). Phase 8 is an **open, iterative track** — this file is where UI features are checked off
as they ship and where new UI ideas are parked before they're specced.

Three parts:

1. **[Shipped](#1--shipped-committed)** — what's built and committed.
2. **[Open / verification debt](#2--open--verification-debt-keeps-phase-8-open)** — what keeps Phase 8 open.
3. **[Backlog](#3--backlog--candidate-ui-elements-the-iterative-pool)** — the pool to pull the next element from.
4. **[Per-feature review checklist](#4--per-feature-review-checklist-run-before-marking-any-ui-feature-done)** — the gate every new UI feature passes before it moves to *Shipped*.

Cross-refs: `docs/ROADMAP.md` (PR table, U-rows) · `docs/specs/feature-phase8-ui-upgrade.md` (design
lock + grill ledger) · `docs/specs/feature-rag-sandbox.md` · `docs/decisions/ADR-010-*` (sandbox
non-persistence) · `docs/decisions/ADR-011-*` (provider switch). When you check an item here, also
add the `docs/DEVLOG.md` entry and update the matching ROADMAP row — this file does not replace them.

---

## 1 · Shipped (committed)

Each box is checked only when the feature is **committed** (not merely staged) and passed the
[review checklist](#4--per-feature-review-checklist-run-before-marking-any-ui-feature-done).

### Chat / answer surface
- [x] **Streaming chat over POST-SSE** — token/step/result events parsed by hand (`api.ts::streamChat`). *(M2/M3 baseline)*
- [x] **U2 — right-aligned, width-capped user bubble; RAG answer stays full-width** — `Turn.svelte` CSS only (`align-self: flex-end`, `max-width: min(72%, 640px)`, tail-cue corner). Commit `7ee1b1e`. *Verified: preview-harness, both themes, mobile no-overflow.*
- [x] **U3 — citation side panel** — click inline `[n]` → slide-over with that source's chunk detail; source cards hidden inline by default. `SourcePanel.svelte` (new) + `Markdown.svelte` text-node linkifier + delegated click; `App.svelte` owns `activeCitation`. Commit `8ba1ffc`. *Verified: preview-harness, mocked SSE ($0).*
- [x] **U4 — "↻ New" conversation reset** — mints a fresh `sessionId` (no backend context leak). `9ce5690`. Subsumed into U5's sidebar.
- [x] **U5 — app shell + conversation history** — left sidebar of past chats (`AnswerRecord.session_id`), reopen read-only, `↻ New chat`, live chat preserved, mobile drawer. Backend `conversations.py` + `GET /api/conversations[/{sid}]`. `9ce5690`. Spec: `docs/specs/feature-conversation-history.md`.
- [x] **Earlier chat-UI refinement pass** — `ee8fe8d` (2026-07-09).
- [x] **Provenance card + low-confidence card** — effective-provider-aware token/`local` suffix. *(M1 baseline; made switch-truthful in `09afd0c`.)*

### Settings drawer
- [x] **Settings disclosure (read-only "Engine" section)** — discloses candidate pool, retrieval weights (now sourced from `config.BM25_WEIGHT`, no longer a hardcoded literal), parent-child, chunk sizes, each labelled with why it's locked / needs a re-ingest. Part of U1, commit `09afd0c`.
- [x] **U1 — RAG sandbox knobs** (ADR-010): `top_k` slider `[1, CANDIDATE_K]`, synthesis-mode segmented (AI/Human), multi-query toggle. Session-scoped, non-persistent, request-scoped through the whole turn — **no module-global mutation** (isolation-guard test). Provenance flags any effective value ≠ locked default. Commit `09afd0c`.
- [x] **U1 — manual System/Light/Dark theme** — `theme.ts` + re-keyed `app.css` (`data-theme` attr wins over the OS media query); `localStorage`, applied in `main.ts` before mount (no flash). Client-only, never a backend setting. Commit `09afd0c`.
- [x] **U1b — two niche sandbox knobs** (ADR-010 amendment): `epistemics_markers_enabled` toggle (contested/superseded chips), `reviewer_evidence_chars` number `[200, 6000]`. Same non-persistent, request-scoped mechanics. Commit `09afd0c`.
- [x] **U1c — live provider + model switch** (ADR-011 v1): `<select>` of configured providers (keyless provider rendered disabled with reason) + model input + Apply; takes effect next turn, no restart; reviewer follows an unpinned switch; persists like `source_dir`. Commit `09afd0c`.
- [x] **U6 — A/B "Test override" compare (retrieval diff, $0)** — per-turn button (shown only when a retrieval-affecting override is set) runs locked defaults vs override, source-set diff badges, no LLM. `compare.py` + `ChatController.compare_retrieval` + `POST /api/compare` + `CompareCard.svelte`. Full-answer 2× compare deferred. `c965418`. Spec: `docs/specs/feature-ab-compare-sandbox.md`.
- [x] **"Point at a folder" source dir + first-run ingest banner** — `/api/settings` + `/api/ingest`. *(M4 data-home flow.)*
- [x] **Reset-to-locked-defaults** button (clears session overrides).
- [x] **S1+S2 — selective ingestion ("Manage files" in Settings)** — `SourceFile` registry + selection-scoped ingest (S1) + sources panel: scan/rescan, status chips, exclude toggle, select-by-status, ingest-selected (S2). `2893544` + `7224f10`. Spec: `docs/specs/feature-selective-ingestion.md`.

### Library space
- [x] **L1 — read-only chunk browser** — Library tab lists docs → parent blocks, each expandable to child chunks. `library.py` (`group_children`/`get_document_chunks`) + `GET /api/library/documents[/{id}]` + `LibraryBrowser.svelte` + the `chat|library` shell switch. Markers/figure thumbnails deferred to L1b. `aa288d9`. Spec: `docs/specs/feature-library-browser.md`. *(chunk-count two-sources blemish + Title—Author display tracked in §2.)*
- [x] **L4 Phase A — Library redesign: nav-tree rail + inventory grid + drill-down** — `LibraryGrid.svelte` + `lib/library.ts` collection model; Sidebar library mode = nav tree (All → Collections empty-state → Types → Added, adaptive); breadcrumb + Back; grid⇄list toggle persisted. Commit `9f597df` (2026-07-15) + layout/tile polish + sort control + active-keyword highlight in the follow-up commits. Phase B (folders) not built. Spec: `docs/specs/feature-library-redesign.md`.
- [x] **Metadata enrichment (deterministic backfill) + keyword de-noising** — wired the unwired `metadata_extractor` onto `Document` + `metadata_enrich.py` sidecar runner (`scripts/enrich_metadata.py`); titles 76/76 on the real corpus (was 0/76 — the enrichment row's premise); `VENUE_STOPWORDS` + repeated-token filter in `keywords.py`. Commit `8f31fe3` (2026-07-16). Local-LLM leftover extraction not built.
- [x] **Manual metadata editing (ADR-013)** — user-editable title/authors/year via the `DocumentMeta` override sidecar (user-entered wins, survives re-ingest; first UI write path) + reveal-in-explorer. `LibraryMetaEditor.svelte`; migration required on old boxes (`python -m doc_assistant.db.migrations`). Commit `e549254` (2026-07-16).
- [x] **Safe-delete (ADR-014)** — Library `⋯` menu → red Delete… + confirmation; source file → OS Recycle Bin (trash-first, abort-on-fail), then row+meta+chunks+figures+cache; `DELETE /api/library/documents/{id}` 200/404/409; new base dep `send2trash`. Commit `95817fc` (2026-07-16). *Live end-to-end delete (recycles a real file) still user-to-run; multi-select bulk delete deferred (needs ADR).*

---

## 2 · Open / verification debt (keeps Phase 8 open)

Built-but-not-fully-proven, or small follow-ups surfaced by the `09afd0c` review. **These are why
Phase 8 is not closed.**

- [ ] **Live-UI smoke test of the sandbox knobs on a real answer turn** — U1/U1b/U1c were preview-harness-verified at *construction only* (no answer turn, $0). Drive one real turn with a non-default `top_k` / synthesis-mode / markers toggle and confirm the `🧪 Session override` provenance note renders and the override actually changed retrieval. *(Cheap now the harness exists — see `.claude/launch.json`.)*
- [ ] **Live-UI smoke test of the provider switch end-to-end** — anthropic→ollama→anthropic was driven through the UI ($0), but confirm the *reviewer-follow* and the `🖥 Local model` usage line on a real flagged answer after a switch.
- [ ] **Live-UI smoke test of the marker chips** — KI-15 backend fix means chips now fire on ~3,334 real chunks, but nobody has confirmed the chip *renders* correctly in the live desktop UI since the fix (phase8 spec "Related backlog").
- [ ] **RG-012 Tier-2** — a cited turn on a clean/frozen box (pends a re-freeze bundling the data-home flow). *(Carried from M4.)*
- [ ] **Review finding — `reviewer_kind="llm_haiku"` is hardcoded** (`chat_controller.py:844`). When the reviewer follows a switch to Ollama, the persisted `AnswerReview` row records `reviewer_kind="llm_haiku"` while `model_name` is (correctly) the live model — a provenance-honesty blemish. Derive the kind from the resolved provider.
- [ ] **Review finding — server-side empty/whitespace model string is unvalidated** — `app_settings.set_llm_selection` validates provider + credential but accepts any `model`; a direct `llm_model=""` POST persists an empty model that `get_llm_selection()` then silently discards. Add `min_length=1` on the wire field or a non-empty `.strip()` check server-side.
- [ ] **Review finding (L1 verify, 2026-07-13) — library chunk counts have two sources of truth** — the doc list's `chunk_count` comes from the SQLite registry (ingest-time `Document.chunk_count`, sums to 11,965 across 76 docs) while the L1 detail header shows live Chroma parent/child counts (30,882 children total); one screen shows both (e.g. "47 chunks" list vs "23 parent blocks · 125 child chunks" detail). Either recompute/refresh the registry count from Chroma, or label the list count for what it is (ingest-time blocks).

---

## 3 · Backlog — candidate UI elements (the iterative pool)

Pull the next element from here. **Add your new UI ideas to this list.** Nothing here is specced
yet — moving one to *in-progress* means writing/locking its spec first (grill it), then building
against the [review checklist](#4--per-feature-review-checklist-run-before-marking-any-ui-feature-done).
Sourced from the phase8 spec's "Related backlog" table + `pr-m1`/`pr-m3` out-of-scope notes.

| # | Candidate UI element | Where tracked | Notes |
|---|---|---|---|
| [x] | **A/B compare sandbox** — locked defaults vs override side-by-side | ADR-010 option 4 · `docs/specs/feature-ab-compare-sandbox.md` | **Shipped as U6 (`c965418`); in §1.** Full-answer 2× compare still deferred (cost-gated). |
| [ ] | **Rich marker UI** — hover a contested/superseded chip → the corroborating/contradicting docs, not just a bare chip | `pr-m1` out-of-scope (tagged PR-M3) | Currently a static chip (`SourceCard.svelte:17-21`). Backend markers already carry the data. |
| [ ] | **Source-evaluation strip (E2, ADR-027 D3)** — always-on per-source row under each answer: coverage/direction (`corroborated`/`unique`/`contested`/`superseded_trend`) + doc year + retrieval-derived signals (`weak_retrieval`, `single_source_risk`, rerank score) + `graph_version` freshness hint | plan 2026-07-21 · `docs/decisions/ADR-027-epistemics-surfacing-split.md` | Precomputed sidecar lookup, $0/turn. **Gated by E1 (KI-8 re-projection + PC `_chunk_key`)**; `contested` stays saturated (53.6% of chunks) until RG-019's denominator. Extends `_build_source_views` + a `SourceCard`/`SourcePanel` sibling. Never hidden by the E3 toggle. |
| [ ] | **Epistemics answer-layer toggle (E3, ADR-027 D2)** — persisted Settings default for markers-in-answers, layered under the existing per-turn `eff_markers_enabled` sandbox knob (U1b) | plan 2026-07-21 · `docs/decisions/ADR-027-epistemics-surfacing-split.md` | Small: settings field + record the effective value in `AnswerRecord` (ADR-011 snapshot discipline). U1b keeps winning for the session. |
| [ ] | **Related-papers panel + citation-network view (E4)** — surface the computed-but-unrouted `similar_docs`/doc_vectors + citation graph | plan 2026-07-21 (`docs/PLAN_2026-07-21_exploration-epistemics.md`) | Backend implemented + script-populated, no endpoint today; ~1 endpoint + 1 component each. Feeds the External-literature-discovery idea (row below). |
| [ ] | **Gap list surface (E5)** — first-class "what documentation to add next" list: kind, concept, status, promote/dismiss | plan 2026-07-21 · ADR-004 | Gaps currently visible only inside the graph view; needs E0's rebuild-coherence fix (KI-17/KI-21) first. Pairs with the "Graph destination" rethink row below. |
| [ ] | **In-app PDF source viewer** — open the cited PDF at the page | `pr-m3` out-of-scope | Deferred at M3, never scheduled. |
| [ ] | **Styled table rendering** in the answer/source view | `pr-m3` out-of-scope | Marker tables render as text today. |
| [x] | **S2 — selective-ingestion sources panel** | `docs/specs/feature-selective-ingestion.md` | **Shipped (S1 `2893544` + S2 `7224f10`); moved to §1** as the Settings "Manage files" section. |
| [ ] | **In-app API-key entry (OS keychain)** — U1c v2 | ADR-011 v2 north-star | Explicitly deferred; needs the keyring decision from ADR-011's open questions. |
| [ ] | **Precise parent-child re-projection for markers** | `pr-m1` ADR-1 option 2 | Backend attribution-quality work, not UI — but gates marker-chip trustworthiness. |
| [ ] | **Conversation rename / delete / search / pin** | `feature-conversation-history.md` out-of-scope | Deferred from U5 (v1 has none). Rename needs a `title` column/sidecar; delete needs a careful "edit history?" call. |
| [ ] | **Conversation retention / prune** (parked from the history grill, H4) | `feature-conversation-history.md` ledger | v1 lists most-recent ~100, no delete/prune. Revisit when `answer_records` grows large — a maintenance increment (CLI or a settings action). |
| [ ] | **Rich / resumable chat rehydration** — claims + reviewer on reopened turns (`AnswerReview`/`AnswerClaim` joins), or resume a past chat as a live thread | `feature-conversation-history.md` Fork B | U5 reopens read-only. The joins + a resumable-session path are the natural follow-up. |
| [~] | **Library space** — a "Library" view: browse ingested docs → their chunks (read chunk text), plus in-app document management + optional chunk annotation | new (2026-07-13, user request); grilled + carved into L1/L1b/L2/L3 | **Carved 2026-07-13.** **L1 (chunk browser) built** (`feature-library-browser.md`, staged, §1 above). Remaining: **L1b** markers + figure thumbnails (reopens when sidecars populate); **L2** in-app ingestion mgmt → adopt `feature-selective-ingestion.md` (S1/S2); **L3** chunk annotation → a new Enrichment-Layer sidecar, **needs its own ADR** (first write path). Pairs with the "In-app PDF source viewer" row. |

| [x] | **Document metadata enrichment (title/authors/year backfill)** | new (2026-07-13) | **Shipped (`8f31fe3`); moved to §1** — titles 76/76. Still open: the local-LLM leftover-extraction stretch (prove on Ollama first). |
| [x] | **Manual metadata editing in the Library** | ADR-013 | **Shipped (`e549254`); moved to §1** — `DocumentMeta` override sidecar (user-entered wins, survives re-runs). |
| [ ] | **Chunk editing + color-coded chunk state** — let the user edit chunk text; color-code **problematic** chunks (flag/highlight) and **modified** chunks (edited vs original) | new (2026-07-13, user request) | **Architecturally heavy — needs an ADR before any spec.** Editing chunks collides with the Enrichment-Layer non-negotiable (derived data never mutates the chunk store): an edit overlay sidecar vs in-place mutation, re-embedding + BM25 refresh on edit, what "problematic" means mechanically (extraction-health? user flag? epistemics?). Supersedes the old L3 "chunk annotation" stub in the Library-space carve row. |
| [ ] | **Epistemics in the Library (L1b reopening)** — surface contested/superseded markers (+ figures) on chunks in the Library browser | user request 2026-07-13 · the existing L1b carve · **diagnosed 2026-07-17** | **⚠ CORRECTED 2026-07-17 — the previous note in this row was WRONG.** It said "the enrichment run hasn't been applied here. First step is running the epistemics build ($0/local) and confirming the sidecar populates." **Running it writes 0 rows.** Measured via a real dry-run (`python -m scripts.compute_epistemics`, free + read-only): `Concept nodes weighted: 26` · **`Contested nodes: 0`** · **`Superseded-trend nodes: 0`** · `Chunks with a claim: **1835**` · **`Chunks marked: 0`**. **The pipeline is HEALTHY — KI-15's fix works** (1835 chunks project fine); what's missing is the **input signal**. `node_weights_for_epistemics` aggregates incident edges' **`stance_by_doc`** into supporting/opposing and documents that *"a stance-less node → `unique`/`stable`"* — and **all 70 `concept_edges` rows have `stance_json = None`** (verified), so no node is ever `contested`, so no chunk is ever marked. **Stance comes from Node B — the LLM relation/stance pass. ✅ CORRECTED 2026-07-17: Node B is BUILT, not "unbuilt" (an earlier note in this row said otherwise — it was wrong).** `src/doc_assistant/knowledge/concept_skeleton_enrich.py` is **code-complete and committed** (pure core + idempotent: it re-derives each edge's annotation from scratch, so re-running is a no-op) and is **runnable today** via **`python -m scripts.build_concept_skeleton --enrich`** (`scripts/build_concept_skeleton.py:150` `if args.enrich: return _run_node_b(...)`, with a `_format_node_b_report` at `:59`). *(The earlier miss came from grepping `scripts/` for a **file** named like a stance runner — the runner is a **flag** on the existing Node-A script.)* **It has simply NEVER BEEN RUN.** **And it is $0, not paid:** `CONCEPT_SKELETON_LLM_PROVIDER` defaults to **local Ollama** (`llama3.1:8b`, `config.py:445-446`) — *not* `LLM_PROVIDER` — and `--apply` routes through `llm.assert_provider_intent` (the KI-4 credit-leak guard). One LLM call **per document**; it **never creates a node or an edge** (by-construction confinement — Node A owns those). **So the real blocker is only: Ollama isn't on this dev box** (it's verified on the separate RTX machine) → **running Node B there is the one prerequisite for every epistemic UI.** Second, independent blocker for the "+ figures" half of this row: see the figures row below. Also known: `superseded_trend` can never fire even *with* stance unless the skeleton carries publication years — the on-disk artifact predates G3 (2026-07-08) so its `meta` has no `doc_years`; a **rebuild** adds them (66/76 docs have a year) **and changes `graph_version`**. Second, independent blocker for the "+ figures" half of this row: see the figures row below. Also known: `superseded_trend` can never fire even *with* stance — the skeleton carries no publication years (`compute_epistemics` docstring). |
| [x] | **Visual identity pass ("sexy pass")** — paper & ink tokens + Lucide icons + Spectral/Inter type + the Provenote rename & app icon | new (2026-07-13, user request) | **COMPLETE:** V1 `35b8627` · V2 `4fd772c` · V3a `181046c` · V3b `487f2df` (all by 2026-07-14). Spec: `docs/specs/feature-visual-identity.md`; installer identity ADR-012; 11-fork grill ledger in the baton. |

| [ ] | **Evidence-only chat mode (no LLM at all)** — retrieval results only, zero interpretation | new (2026-07-13, user request) — **PLANNED 2026-07-17 = PR-1** in the plan file (see §3 note below) | **The "~90% exists" claim is broadly TRUE but two of its details were wrong (measured 2026-07-17):** (1) *"in Settings"* undersells it — `synthesis_mode` is already a **request-scoped per-turn override** (`chat_controller.py:759-761`, ADR-010 honoured, no global mutation), surfaced at `Settings.svelte:384-403`, scope = per-**app-session** (deliberately not conversation state, `App.svelte:477`). (2) *"renders evidence-only"* — it renders as a **concatenated markdown string** with an emoji+em-dash prefix (`chat_controller.py:1055-1058`, missed by the de-tell pass because it lives in `src/`); **`result.mode` is DEAD on the frontend** (live on the wire at `types.ts:30`, **zero** consumers) — a free field to consume. **Decided (user, 2026-07-17): force true $0 + inform, never gate.** Gate the rewrite at `:788` and force multi-query off at `:797` (which also fixes an existing raw-vs-resolved-override bug there). **Why forcing is nearly free:** human mode writes `"(human mode: evidence only)"` into history (`:1053`), so in a pure-human chat the rewrite LLM reads a **zero-information stub** — it is *structurally incapable* of resolving a pronoun. It costs money to accomplish nothing. **And the turn already lies:** `_human_result` hardcodes `UsageView(0, 0, …)` (`:1063`) while the rewrite really spends tokens — the fix makes an existing lie true. **Honest residual:** *mixed*-mode chats (AI turn 1 → human turn 2) genuinely regress. **Test trap:** `expand_query` (`pipeline.py:302`) takes **no counter**, so a `counter.total()==0` assertion **cannot** catch the multi-query leak — spy the pipeline (`FakeRAG.rewrite_calls`), not the counter. No ADR (ADR-010 already covers request-scoped knobs). |
| [ ] | **Unconstrained mode — corpus restraint off, measurements on** — let the model answer from internal memory; keep retrieval + the full integrity layer (provenance, reviewer, citation checks) running to *measure* what ungrounded answers do | new (2026-07-13, user request) — to plan (grill when picked) | Needs a spec + grill: two variants to resolve (no-retrieval pure-LLM vs retrieval-shown-but-prompt-unconstrained), and the measurement design (reviewer still grades claims against corpus evidence → a groundedness delta per turn). **Pairs naturally with the deferred full-answer A/B compare** (constrained vs unconstrained side-by-side). Paid-LLM per turn → KI-4 rules: prominent paid-mode badge, verification cost-gated (needs a model). |
| [ ] | **External literature discovery** — use the enrichment layers (epistemics, authors, keywords/concepts, citations, citation graph, concept graph) to find related papers/data beyond the corpus | new (2026-07-13, user request) — to plan; phase-level track (ROADMAP bullet added) · **⛔ B13 of the 2026-07-17 concept-graph grill PARKED HERE — this row is now the graph's action half** | **B13 (user, 2026-07-17, from the concept-graph grill):** *"give the user the option to **download and find more information to complete the graph**… we will need a **provider list, and a quality list**."* **This closes ADR-004's loop as written** — gaps surface *"directions for exploration the user did not think to look"*, and the response to a gap (`single_source` on concept X) is to **go acquire a corroborating source**: gap → find → download → ingest → **rebuild (7.1 s, measured)** → the gap closes. So this row is no longer a speculative "discovery" nicety — **it is the action slot the concept-graph view exists to trigger**, and the graph is its entry point. **Already de-risked:** the **transport is spiked** (stdlib `urllib` → `api.crossref.org` works on this proxy box **with and without** truststore, **25/25 DOIs, 0 failures** — see the KI-10 addendum; **stdlib, not httpx**, and behind a named module-level seam or the cpc `test_api_check` gate trips on `urllib.request`); `sources_manifest.py:278-285` `_http_get` is the **in-repo precedent to copy** (scheme guard + UA + timeout + `# nosec B310` + checksum verify + a mockable seam); and **`GapStatus.promoted` already exists** as the lifecycle slot to attach to (`build_gaps` persists status across rebuilds). **CONSTRAINT ON THE CONCEPT-GRAPH SPEC: model a gap as an object with an ACTION SLOT so this attaches later without rework.** **Still needs its own ADR** (provider list + **quality list** — which sources are trustworthy — + caching, provenance of an acquired doc, rate limits, and the local-first "degrade, don't block" rule). Shares infra with the metadata **ADR-016**/PR-8 Crossref runner — **decide whether they share one client module before both are written.** Original scope below stands. Scope: **legitimate open-access APIs only** — OpenAlex, Semantic Scholar, Crossref, arXiv, Unpaywall, CORE/DOAJ/EuropePMC (Sci-Hub excluded: distributes copyrighted papers without authorization). Shares lookup infra with the metadata-enrichment row (arXiv/Crossref by ID/DOI) — build that first. **Needs its own ADR:** first outbound-network feature beyond the LLM APIs on a local-first app (proxy/offline constraints, caching, rate limits, provenance of external hits). |
| [ ] | **Global CLI** — `doc-assistant ask "…"` callable from any terminal (installed on PATH), disabled by default | new (2026-07-13, user request) — **PARKED: post-review phase (user call)** | User's own hesitation on the record ("don't know if this is a good idea"). A repo-local CLI already exists; this is packaging/PATH + an enable flag. Thin shell over `ChatController` per the house pattern — cheap once the API surface stabilizes. Revisit after the review-phase debts (§2) clear. |
| [ ] | **MCP server** — expose the RAG as MCP tools (local stdio: retrieve/ask/library lookups) so Claude & other agents can query the corpus | new (2026-07-13, user request) — **PARKED: post-review phase (user call)** | Natural fit: a retrieval-only tool is $0 (pairs with evidence-only mode); an answer tool inherits the provider/cost rules. Post-review because the tool contract should freeze after the API surface stops moving. Same thin-shell rule as the CLI. |

| [x] | **Faceted keyword filtering → two-pane overlay** — searchable keyword list + live doc preview; AND semantics; grey-out unavailable | new (2026-07-16) · `docs/specs/feature-keyword-filter-overlay.md` | **Shipped `ce8b112`** (frontend-only). Pure `facetFilter`/`keywordFacets` in `lib/library.ts`. |
| [ ] | **Tag families / keyword normalization** *(**PR-1 → PR-2.7 all shipped**; 2026-07-20 brought PR-2.5 write-path defects D1–D5 + the first frontend tests, PR-2.6 family-aware tiles (D6), PR-2.7 the Manage view at scale (F1–F4). Only PR-3, the optional LLM assist, stays parked)* — one canonical tag with many surface forms (LLM/LLMs, connectome≈connectomics), user-editable; a deterministic **detection** pass (morphological + `bge` embedding) proposes merges; an *optional, gated* LLM confirm pass (prove on Ollama first) | new (2026-07-16, user feedback) · **PR-1 ✅ SHIPPED `0c3b0d4` · PR-2 ✅ SHIPPED `0af43db`** (2026-07-17) — ADR-015 + `feature-tag-families.md`. **Row stays open: PR-2.5 (hardening) + PR-2.6 (family-aware tiles) SCOPED 2026-07-17** from a post-commit review — 6 verified defects (D1–D6, repros in the spec), all in the **write paths**; the read path (facet math, union-find, thin shell, default path) reviewed clean and the feature verified working live. **D1/D2 corrupt data beyond the feature** (duplicate `Concept` labels break `promote_keyword` repo-wide; rename silently drops the family's own canonical) and sit on the natural Detect→Accept→**Rename** flow. PR-3 (LLM) still parked | *Takes advantage of* `Concept`/`ConceptAlias` (family = a `Concept` + alias rows whose aliases are `Keyword` names) — **not** the concept-graph UI (graph/epistemics own a later track over the same rows). Filter = **keyword-name grouping** (union of `document_keywords`); curation in a dedicated **"Manage keywords" view**; overlay renders a family as an **atomic entry** (canonical + N forms). **PR-1 built + verified live** (76-doc corpus, 26 pre-existing curated concepts incl. `Large language model`←`llm`/`llms` collapsing correctly): 6 new `/api/library/keyword-families` routes, `LibraryManageKeywords.svelte`, family-aware `keywordsOf` accessor on `facetFilter`/`keywordFacets`. **PR-2 built + verified live**: `keyword_families.py` pure core (Tier 1 morphological stem-match, Tier 2 bge cosine clustering via union-find + edit-distance as a supporting signal), `POST /api/library/keyword-families/detect` (reuses the loaded embedder, no new model load), `scripts/detect_keyword_families.py` (report-only, no `--apply`), a "Detect proposals" section in the Manage view (Accept/Dismiss per proposal). Live run on the real corpus found a genuine proposal (`pvpo`≈`avpv pvpo`, 0.77 conf) via both the CLI and the app. Carve: ~~PR-1 end-to-end manual~~ → ~~PR-2 detection~~ → **PR-3** LLM (parked). **Also carries "favorite keywords"** — a promoted `Concept` = a de facto pinned/favorite (deferred from the keyword-overlay grill). |
| [ ] | **Missing-source handling + library-only delete** — badge documents whose source file was deleted outside safe-delete (derive live via `resolve_source_path`, like `SourceFile`'s `missing` status — no new column); add a second delete mode **"Remove from library only (keep the file on disk)"** (the inverse of ADR-014's trash-first delete); keep-chunks-but-flag-not-re-ingestable | new (2026-07-16, user feedback) · **extends ADR-014** · **PLANNED 2026-07-17 → SPLIT into PR-3 (fix `resolve_source_path`) / PR-4 (badge) / PR-5 (library-only delete)** — see the plan file (§3 note below) | **Measured 2026-07-17:** **0/76 docs are missing** → the badge has **nothing to show live**; verify by *synthesizing* (rename a source file, check, rename back) + canned-fetch, per the "0/76 metadata" precedent. Live existence check for all 76 = **1.0 ms** (4 call sites, never per-render) → **derive live is sound**. **`resolve_source_path` has 3 real bugs and must be fixed FIRST (PR-3) — the badge is wrong without it:** (a) fallback uses `config.DOCS_PATH`, not `app_settings.get_source_dir()` → **false "missing"** on a custom source dir; (b) fallback is **flat**, not recursive; (c) `Path.exists()` swallows `OSError` → permission-denied/locked/offline-UNC is **indistinguishable from deleted** → return type must widen to `tuple[Path|None, SourceState]` (PRESENT/MISSING/UNREADABLE). **Do NOT reuse `scan_sources`** for the badge — it **WRITES** (upserts rows, refreshes `last_seen`); a read-only list endpoint must not mutate the registry. **Badge = a dot, not a word-chip** — `tilehead` has no reserved height and no wrap control, so a "MISSING" chip breaks the uniform fixed-footprint tile height (reuse `.editmark` geometry + `Sources.svelte:326-329`'s existing `.chip.c-missing` colour language). **⚠ The substance of library-only delete is NOT the trash-skip — it's `SourceFile.excluded`.** `delete_document` never touches `SourceFile`; without exclusion the next `just ingest` **re-adds the doc** (the hazard ADR-014's own Context `:16-18` names) — *a bug with a button*. Seam exists: `registry.set_source_meta(session, rel_path, excluded=True)` (`ingest/registry.py:246-262`); **exclude BEFORE deleting the row** (ADR-014's Decision B is abort-on-fail-first). **Restore already exists + is discoverable:** `Sources.svelte:78-80` `toggleExclude` → `main.py:698-704` — *that mechanism is the whole reason ADR-014's rejected A1 can be revived as a second mode.* Residual: `--files` **overrides** `excluded` (must be in the dialog copy). **`is_archived` is a TRAP, not a shortcut** — ~12 read paths filter it and **nothing ever sets it**, but **the retrieval pipeline has NO `is_archived` filter and Chroma chunks carry only `doc_hash`**, so an "archived" doc would **vanish from the Library while its chunks kept being retrieved and cited**. **UI: a checkbox in the existing dialog, NOT a 4th menu item** (`openMenu` hardcodes geometry for 3 items, `LibraryGrid.svelte:68-69`; two adjacent red Delete items is a footgun). Prereq: `api.ts:118-124` discards the error `detail` → reuse the existing `errorDetail()` helper (`api.ts:358-372`). `delete_document` today always trashes the source and aborts if it can't (`library.py:316`). An orphaned doc stays fully queryable but nothing surfaces it. **Needs an ADR amendment** (a second delete contract). |
| [ ] | **Extended document metadata + web autocomplete** — surface the stored **DOI**; add `journal / url / abstract / article_type` (research/review/…); extend the `DocumentMeta` override sidecar to match; **Calibre-style web autocomplete** (fetch metadata by DOI/title from Crossref / OpenAlex / arXiv) | new (2026-07-16, user feedback) | **SPLIT 2026-07-17 (user) into PR-2 (surface stored DOI) / PR-6 (ADR-016) / PR-7 (fields+editor) / PR-8 (Crossref runner)** — see the plan file (§3 note below). **Measured 2026-07-17, and it corrects two claims in this very row:** (1) **"first outbound network call beyond the LLM APIs" is FALSE** — `sources_manifest.py:278-285` `_http_get` **already** does `urllib.request.urlopen` (scheme guard + UA + timeout + `# nosec B310` + checksum verify), as does `scripts/download_corpus.py:72-73`. The **defensible** claim is *"first outbound call from the API **serving path**"* (`apps/api/` has zero httpx/requests/urllib imports). **`_http_get` is a ready-made precedent to COPY, not a novelty to argue against.** (2) **`article_type` is NOT text-extractable here — measured 0/76.** (An earlier claim that `_SKIP_HEADINGS` "already parses the signal and throws it away" was wrong: `_is_skippable_heading` (`metadata_extractor.py:125-138`) returns a **bool skip predicate**, and `_SKIP_HEADINGS` mixes types ("research article") with **section names** ("abstract","methods","main") — it never fires as a taxonomy.) **Live corpus:** `doi` **25/76** (stored, indexed, and **100% invisible** — `grep -r doi apps/` → **zero**; `get_document_details` is called only as a 404 existence check at `main.py:454,464` with its return **discarded**), of which **2 are `type: component`** (a figure/supplement DOI — wrong for the paper; an argument **for** surfacing). `notes` **0/76 with no writer** → **don't surface it read-only**. `journal`/`url`/`abstract` columns **don't exist**. **Local-text yield is hopeless** (journal 5/76 · url 1/76 · abstract 3/76 · article_type 0/76) → **Crossref beats it on all four** (of the 25 DOIs: URL 25 · type 25 · container-title 21 · abstract 12) → **ship journal/url/article_type, defer abstract**. **Migration is ~6 appends to `db/migrations.py:25-34` `_ADDITIVE_COLUMNS`, not a new table** — the exact precedent is `("conversation_meta","title_override","TEXT",None)` at `:33`; the API does **not** `create_all` on startup, so the migration must be run by hand. **Transport DECIDED: stdlib `urllib` behind a named module-level seam, not httpx** — see the KI-10 addendum (2026-07-17); the **cpc-test-api-check gate** (`test_api_check.py:26`) trips on the literal string `urllib.request` in any test file, which is *correct pressure*: the seam must be monkeypatchable by name (precedent: `tests/integration/test_sync_sources.py`). **Do not use the `cpc: allow-live-api` pragma.** Still needs its own **ADR-016** (local-first degrade-don't-block, caching, provenance, rate limits — Crossref's polite pool wants a `mailto:` UA, **env var, never hardcoded**); shares lookup infra with the parked "External literature discovery" row. When `article_type`/`year`/`journal` land as **filter dimensions**, they reopen the parked "general Filters hub" question (`docs/specs/feature-keyword-filter-overlay.md` Parked). |

| [x] | **Family-aware grid tiles** — render a tag family as its atomic canonical chip on Library tiles (ADR-015 T6) | PR-2.6, 2026-07-20 | **Shipped** — 22/22 tiles highlight a family selection (was 0/25). Frontend-only; spec `docs/specs/feature-tag-families.md`. |

| [ ] | **Manage view at scale** — the Manage-keywords view + filter overlay hold up at ~100 families: move the "Manage keywords…" trigger out of the scroll region, autocomplete the "New family" canonical against existing families (navigate, don't duplicate), searchable pickers instead of flat pools, and **demote the 1-doc long tail** behind a "Show rare (N)" toggle | new (2026-07-17, user feedback on the live view) — **= PR-2.7 in `feature-tag-families.md`** | **Measured on the live corpus:** 60 keywords surface, **30 (exactly 50%) sit on a single document**; `FAMILIES (26)` shows only ~6 real families, the rest 0-member concepts inherited from concept-graph seeding (several with 0 docs, e.g. `BERT`). **Principle: a facet exists to partition a set — a 1-doc facet doesn't partition** (selecting it yields one doc, which search does better), so rare keywords have near-zero *filtering* value whether they're junk or gold; the threshold sweeps up the ugly strings **without classifying them**. **Demote, don't delete** — the tail is mostly *real* specialist vocabulary (`va1v`/`dl5`/`osns` = Drosophila glomeruli + neuron classes; `avpv`/`pvpo`/`mpoa` = hypothalamic nuclei; `rabv` = rabies tracing), and the overlay's existing search box is the escape hatch. **The canonical-autocomplete is also defect D3's fix** — PR-2.5 owns the `library.py` invariant, PR-2.7 owns the control that stops the user reaching it; don't build it twice. Frontend-only, `$0`, no ADR |
| [ ] | **Keyword truncation fix (extractor)** — `_TOKEN_RE` splits on `.` and `/`, mangling real domain terms: **`16p11.2` → `16p11`** (autism CNV) and **`C57BL/6` → `c57bl`** (mouse strain, 7 docs) | new (2026-07-17, traced from user feedback) — **NOT UI**: `src/doc_assistant/knowledge/keywords.py` | Found while tracing the "wtf is 102ff/16p11" question to source docs. `_TOKEN_RE = [a-z0-9]+(?:[-+][a-z0-9]+)*` (`keywords.py:36`) allows `-`/`+` as internal joiners but not `.`/`/`, so dotted/slashed identifiers are cut at the separator. **Touches the ingest path, not the UI** — a fix means re-running keyword extraction over the corpus (76 docs) and re-checking the de-noising interaction (`VENUE_STOPWORDS` + repeated-token filter). Needs its own carve + a before/after keyword diff on the real corpus; **note the casefold/ASCII invariant** that the tag-families review relies on (`Keyword.name` is always ASCII-lowercase, which is why SQLite `lower()` == Python `.casefold()` there) — widening the token class must not break it. Deferred behind PR-2.5/2.6/2.7 |
| [ ] | **Keyword suppression (hide true artifacts)** — a user-editable "hide this keyword" list for genuine garbage, so it leaves the facet list permanently rather than only being demoted | new (2026-07-17, user request) — **needs a design decision before a spec** | Distinct from PR-2.7's *demotion* (which is display-only + reversible-by-search) and from tag families (*synonym collapse*). Targets the real artifacts: `mathrm` (LaTeX `\mathrm` leak), `professium`, `outflux` — **all three from one 1952 scanned Hodgkin-Huxley paper** — plus `102ff` ("p. 102ff" citation artifact), `fne-tune` (OCR of "fine-tune"), `neurosc`. **Open question = where suppression lives:** a **sidecar table** (user-override precedent: `DocumentMeta`/ADR-013) is the likely shape — it's a *user override*, not derived data, so the Enrichment-Layer Pattern doesn't govern; a code-level stoplist (`VENUE_STOPWORDS` precedent) is wrong for user-editable data. **Rejected already: modelling it as a "hidden" family** — abuses ADR-015 (a family means *synonym collapse*; overloading it corrupts every consumer of those rows). New table + migration + a UI write surface → **wants an ADR or at least a spec section**; may pair with the extractor fix above (fix what you can, suppress what you can't) |

| [ ] | **Concept-graph view (Obsidian-like)** — an interactive graph of the curated concept vocabulary: nodes = `Concept`, edges = `concept_edges`, click a node to see its documents/chunks; the "explore my corpus by idea" surface | new (2026-07-17, user request) — **the phase's biggest unbuilt feature; DATA IS READY** | **Measured live 2026-07-17: `concepts` 26 · `concept_aliases` 17 · `concept_edges` **70** · `concept_presence` **222** · `doc_similarities` **760** · `skeleton.json` present (44 KB, `graph_version 055312c8c15a7e69`). Nothing blocks it.** **ADR-015 explicitly RESERVED this track** — tag families *takes advantage of* the vocabulary and is **not** the graph UI ("graph/epistemics own a later track over the same rows"), and PR-1/PR-2 just made `Concept` a first-class UI citizen (CRUD + detection + the Manage view + `keyword-families` routes). So the read model, the write path, and the vocabulary all exist. **Node A is zero-LLM and free** (`build_concept_skeleton`: curated label/alias string-match presence + chunk co-occurrence edges + citation/similarity provenance + Louvain communities, deterministic + byte-stable, Enrichment-Layer). **Watch:** edges are **stance-less** (`relation`/`stance_json` all `None` — that's Node B, unbuilt) so the graph shows *association*, **not** agreement/disagreement — the UI must not imply epistemic stance it doesn't have. `node_weights_for_epistemics` already computes per-node weights (contested/stable/unique) and Louvain communities exist → free colouring/clustering signal. **✅ GRILLED 2026-07-17 (`grill-me`) — 12 branches, 11 RESOLVED, 1 PARKED, 0 open. Full ledger in the session baton.** **✅ ADR + SPEC WRITTEN 2026-07-17: `docs/decisions/ADR-017-concept-graph-ui-boundaries.md` (accepted) + `docs/specs/feature-concept-graph.md` (design-locked) — the spec is the code-level contract; read it before building.** **✅ PR-G1 (serve) + PR-G2a (the view — concept index + gap lens + seeded-SVG ego graph + chunk nav) BUILT & VERIFIED LIVE 2026-07-17 (staged, uncommitted; see the DEVLOG). Remaining: PR-G2b (gaps as a destination + triage sidecar, ADR-017 C1), PR-G2c (Library doc→concepts entry), PR-G4 (Node B on the RTX box → the reserved edge-stance encoding).** **⚠ THE GRILL OVERTURNED ITS OWN ROOT QUESTION:** I opened intending to argue against a pretty-but-useless Obsidian clone; **ADR-004 answered first — *"Phase 7's stated purpose is gap detection — and the project's north-star reason for it is to surface what the user (and the LLM) cannot see: concepts the corpus under-supports, claims it cannot source, and directions for exploration the user did not think to look."*** The gap layer is **BUILT + Ollama-validated with a recorded baseline** (Tier-1 + Tier-2a floor SPRINT-002/G2; stochastic ceiling SPRINT-005/G5) — and has **0 rows and ZERO UI**. **A free dry-run found 14 REAL GAPS on the live corpus:** `under_connected` 5 · `unsourced_claim` 4 · `single_source` 3 · `thin_bridge` 2. **So the graph has a payload today — it is not decoration.** The user's own framing independently matched the detector: *"technically, having a single source is not good."* **THE JOB (B1) = corroboration + coverage + navigation**, not browsing. **Key resolutions:** **B3 ego-first, depth-1** — measured **22% density and `Embeddings` alone has degree 20/25 (touches 80% of the graph; its depth-1 ego = 81% of all nodes)** while the **median ego is 6 nodes**, so the hairball is real *today* at n=26; **B4** sequence = concept index + gap lens + ego + chunk-nav → gaps as a first-class home → Library doc→concepts entry; **B5/B6 read-only for the VOCABULARY + deep-link to Manage-keywords** (you edit the source and regenerate, never the derived artifact; one write surface for the invariant PR-2.5 is repairing); **B7** node→`doc_ids[]`→`concept_presence.chunk_keys` — **verified 1781/1781 (100%) resolve** (ADR-4 key `{document_id}:p{parent_index}`); **B8 staleness indicator + in-app Rebuild button (202+poll)** — **measured rebuild = 7.1 s, zero-LLM**, and `POST /api/ingest` 202 + status-poll is the exact precedent (CLI runner stays canonical ⇒ Enrichment-Layer holds); **B9 gaps encode on NODES** (every `Gap` is anchored to `concept_id`, even `thin_bridge`) **and stance on EDGES** ⇒ no palette collision, and B3 defused the 3-hue gap; **B14 gap triage (dismiss/promote) — `GapStatus` already exists and `build_gaps` deliberately persists status across rebuilds**, so the sidecar was *designed* for it (this refines B5: read-only covers the **vocabulary**, not the gaps sidecar). **⛔ PARKED — B13 the gap→acquisition loop** (user: *"give the user the option to download and find more information to complete the graph… we will need a provider list, and a quality list"*): closes ADR-004's loop, merges with the parked **External literature discovery** row, **needs its own ADR**; **transport is already spiked** (stdlib urllib → Crossref, 25/25). **CONSTRAINT ON THE SPEC: model a gap as an object with an ACTION SLOT so the loop attaches later without rework — `GapStatus.promoted` is that slot.** **✅ RG-014 RUN 2026-07-17 — VERDICT: ~50% precision (8/14 defensible). B1 SURVIVES but NARROWS.** Procedure: fresh skeleton (`b59a4aa6afa77978`) → dry (**still 14 — stable across the rebuild, not a staleness artifact**) → `--apply` (**14 rows persisted**) → re-run (**14, no duplication — idempotent**). **Per kind:** ✅ **`single_source` (3) TRUE POSITIVE and the product thesis** — `Res2Net` only in the Res2Net paper, `SBERT` only in the Sentence-BERT paper, `PHATE` only in one paper: each a method known *solely from its originating source*, exactly *"having a single source is not good"*, and directly actionable via B13. ⚠️ **`unsourced_claim` (4) real but ~33% contaminated** — **12 of the 61 underlying claims are markdown HEADINGS** (structurally uncitable ⇒ permanent false positives) + 8 fragments → **new segmenter-bug row below**. ❌ **`under_connected` (5) mostly NOISE at n=26 — and it's the largest kind**: it measures **graph degree**, dominated at a 26-concept vocabulary by **vocabulary sparsity, not corpus coverage** — **`Tractography` (10 docs, deg 2)** and **`Motor control` (13 docs, deg 2)** are among the **best-sourced** concepts yet flagged as gaps; 2 of 5 duplicate `single_source`. *(Should improve as the vocabulary grows — noise at this size, not permanently.)* ⚠️ **`thin_bridge` (2) redundant + half-misleading** — both from **one edge**, and it flags both endpoints so **`Embeddings` (degree 20/25, 32 docs — the most-connected node) is reported as a "thin bridge" gap.** **⇒ THE SPEC-SHAPING FINDING: the STRONG kinds are LIST-shaped (`single_source`, `unsourced_claim`); the WEAK kinds are GRAPH-shaped (`under_connected`, `thin_bridge`). The graph is NOT the primary renderer for the gap payload — a list is. Do not lead with `under_connected`.** (B7 navigation value is independent and verified.) **⚠️ B14 REOPENS — the grill's reasoning was WRONG:** `gaps.py:257` `delete(GapRow).where(GapRow.determinism == "deterministic")` — deterministic gaps are **delete-and-replace**; only `_write_stochastic_gap_rows` (`:273`) is *"status-preserving"*. **Verified live: a `dismissed` gap reset to `surfaced`.** **All 14 are deterministic** ⇒ **dismissing any of them is futile, and rebuild is part of the acquire loop** ⇒ triage needs a durable store keyed on `(concept_id, kind)` mirroring the stochastic upsert — **decide in ADR-017.** **Also open: `under_connected` needs a corpus-vs-vocabulary guard** (gate on doc-count, or defer the kind). Live state: `gaps` = **14 rows**, all `surfaced`, all deterministic. **Confirmed stale:** the live rebuild moved `graph_version` `055312c8c15a7e69` → `b59a4aa6afa77978` and **added `doc_years`** (so `superseded_trend` becomes possible once stance lands). Prior carve (PR-G1 serve / PR-G2 view / PR-G4 Node B) survives, now widened by B2: **PR-G1 serves skeleton + gaps + presence**. **Decisions taken (user):** (1) **hand-rolled SVG + a seeded force layout, NO new dependency** — the frontend has **exactly one** runtime dep (`marked`) and `dist` is **101 KB**; the Tauri CSP is `default-src 'self'` with **no `unsafe-eval`**, so an eval-using lib breaks **only in the packaged build** (dev-mode Vite won't catch it); and decisively, with **zero frontend tests** and screenshots timing out, **SVG's DOM is the only verification surface** (canvas/WebGL ⇒ literally unassertable here). Cytoscape ~400 KB = **4× the whole bundle**. (2) **Association-only** — see the encoding note above. (3) **One vocabulary: a family IS a concept** — do *not* filter by `source`; ADR-015 chose the sharing deliberately and "Large language model" collapsing `llm`/`llms` genuinely *is* a concept. **The real defect is that the skeleton is a build artifact, so the graph silently lags user edits → surface staleness + a Rebuild action** (inform, don't block). **Resolved open questions:** it's a **full view** (`mode === 'graph'`), **not a modal** — there is no reusable modal shell (scrim+dialog hand-rolled in **6** components, all `min(84vh,620px)`-capped transient *tasks*; a graph is a *destination*); the shell change is small (the `'chat'|'library'` union appears in exactly **4** places: `App.svelte:118,562,800` + `Sidebar.svelte:40,50`) and wants `main.wide` (`:1160`); click-through reuses `openDocument()` (`:580`) since `doc_ids[]` is already on the node. **Build notes:** copy `App.svelte:301-321 startResize()` for pan (wheel-zoom needs `passive:false`, a deliberate divergence from `LibraryGrid.svelte:87-97`); emit real `<line>/<circle>` children per `Icon.svelte`'s pattern — its "no `{@html}`" is **SVG-namespace safety, NOT CSP/XSS** (`Markdown.svelte:107` uses `{@html}` in prod). **⚠ Palette gap:** only **3 non-semantic hues** exist (`--accent`/`--lavender`/`--ok-fg`) and there are exactly **3 communities** — that's luck, not headroom (`--promote-all` → ~86 nodes); add a categorical palette or derive via `color-mix`, **zero hardcoded hex**, and **reserve `--danger`/`--warn-fg` for stance**. `IconName` is a closed 26-name union with **no graph glyph** — add a Lucide path. **⚠ Traps:** `skeleton.json` is **gitignored + STALE** (mtime 2026-07-02, predates G3 2026-07-08 → `meta` has no `doc_years`); a rebuild adds them **and changes `graph_version`** → **never hard-code `055312c8c15a7e69`**; a fresh clone has **no skeleton at all** (the empty state is the normal first-run path); **`data/graph/graph.json` is a stale EMPTY DECOY** (0 nodes, Jun 15 — residue of the retired `concept_graph.py`; reading it renders an empty graph that looks like a layout bug). **There is no `load_skeleton()`** (`skeleton_from_dict:597` exists but nothing reads the file) and **zero API routes serve graph data** — the read model is all to build; note node ids are **UUIDs** with labels only on nodes, **the exact id-space mismatch that caused KI-15** — pick one wire id space and document it. **Weight-as-thickness is nearly flat** (2.377–2.949) and a provenance legend would have **one state** (all 70 edges are `{cooccurrence, similarity}`; **0 resolved citations** so the `citation` token can never fire). Communities are **positional, not identity** — never persist a preference against a community id. **Design for 300–500 nodes, not 26.** |
| [ ] | **App shell → chat-first layout** *(**(a)+(b) SHIPPED 2026-07-21** — staged; only (c) remains)* — chat-first shell; **demote Graph out of the top-level nav** (it is a co-equal peer of Chat/Library today and shows nothing until the concept skeleton is built). Sub-items: **(a) ✅ global search as an overlay** reusing the modal shell (scrim + centred dialog + Esc), not a page — built as a **navigation** search over chats + documents (title/filename/authors/keywords), header button **+ Cmd/Ctrl-K**, grouped/capped results, empty→recent chats, keyboard-first; **(b) ✅ collapse-the-sidebar** button in the header (desktop-only, persisted, mobile drawer untouched); **(c) ⏳ demote Graph out of the nav — STILL OPEN, needs `grill-me` first** (where Graph goes is entangled with the empty-Graph → per-folder-concepts fork, ADR-025 fork 5, parked; the user is explicitly undecided — do not move it without settling that). Spec: `docs/specs/feature-app-shell-search-collapse.md`; new pure module `lib/search.ts` (+ `search.test.ts`, 11 tests) + `GlobalSearch.svelte`. | new (2026-07-20, user — closing note; reference: the conventional chat-app sidebar — brand · search · collapse, then New chat / Library / …) | (a)+(b) were **shovel-ready frontend polish**, low risk, no backend — now done. **(c) is the open part** — see the row below; do not move it without settling that. Memory: `ui-direction-app-shell`. |
| [ ] | **Graph destination — rethink placement + the empty state** — the empty Graph page reads as broken; the user would rather it resemble the **Library side-panel**. Candidate: **per-folder concepts** (= ADR-025 **fork 5**, per-folder enrichment — currently *parked*), but the user is **explicitly undecided** ("I don't know which way will be best"). | new (2026-07-20, user — closing note) | **DESIGN FORK — grill before building.** Couples three parked/unbuilt things: graph nav placement, ADR-025 fork 5 (per-folder concepts), and the fact that the graph only fills once the skeleton exists (KI-25). Run `grill-me` → amend ADR-025 / write an ADR; do **not** crystallise a direction from this note alone. |
| [ ] | **Post-play UI polish (user, 2026-07-21 — from using the app after E2)** — three items to tackle **after the current E-track building phase**: **(a) fix the collapse-sidebar button** (the (b) sub-item of the "App shell → chat-first" row above); **(b) fix the global search bar** (the (a) sub-item of that same row · `GlobalSearch.svelte`); **(c) improve navigation + feedback in the Graph view** (`ConceptGraph.svelte` — pairs with the "Concept-graph view" and "Graph destination" rows). | new (2026-07-21, user — closing note) | **Deferred to after the E-track (E3/E4/E5).** (a)/(b) are **defects on the shipped app-shell (a)+(b)** (`d3634c9`) — ⚠ **exact repro/symptom still TBD** (capture it before fixing, per the house diagnose-first style). (c) is an enhancement on the built concept-graph view; likely folds into the "Graph destination" grill. |
| [ ] | **Concept hierarchy — implement ADR-019** — the concept taxonomy / classification layer (ANZSRC backbone) is **accepted + designed but never built**; the user named it as outstanding on 2026-07-20. | ADR-019 (accepted 2026-07-18, unbuilt) | Its own build track; read `docs/decisions/ADR-019-concept-taxonomy-classification-layer.md` first. Note its own recorded caveat: Node B yields ~1 usable hierarchy edge per rebuild, so the *curated* backbone is load-bearing, not the derived edges. Likely interacts with the graph-destination fork above. |
| [ ] | **Surface figures + tables in the reading surfaces** — render extracted figures (cropped PNGs) and tables inline in answers, the chunk browser, and the doc view | new (2026-07-17, user request) · partially overlaps the existing "Styled table rendering" + "Rich marker UI" rows | **⚠ Measured 2026-07-17 — BOTH are blocked on data, and they block differently.** **Figures:** `figures` = **0 rows**, and **0 chunks carry `chunk_type` in *either* Chroma index** (`data/chroma` 11 965 · `data/chroma_pc` **30 882** = the live one the API loads — all `(none)`). The `chunk_type='figure'` ingest path exists (`ingest/__init__.py:220-271`) but has **never produced a chunk on this corpus**. Unblocking = `extract_figures` **+ `describe_figures`, which is by its own docstring "the project's only **paid, API-only** enrichment"** (VLM, gated by `MAX_VLM_CALLS_PER_DOC`, dry-run default) → **a real cost decision under the repo's cost discipline: prove offline + on a local model first, never burn paid API at scale on unproven extraction.** **Tables:** extraction code **does** exist (`src/doc_assistant/ingest/tables.py`, `scripts/extract_tables.py` / `extract_tables_marker.py` / `eval_marker_tables.py` / `debug_tables.py`) but **no `tables` table exists in the DB and no table chunk is indexed** — so **diagnose first**: where were tables meant to land, and why did they never land? (`document_parts` is also 0 rows.) **Do not plan the UI until the data question is answered for each half separately.** |
| [ ] | **Highlight citations + sources in answers** — make inline citations and their source passages visually traceable (hover/click a citation → the exact chunk; the passage highlighted in context) | new (2026-07-17, user request) · adjacent to the parked "In-app PDF source viewer" row | **⚠ CORRECTED 2026-07-17 — an earlier note in this row cited "`citations` = 3918 rows" as this feature's data. That is the WRONG table.** Two different things are called "citations": (1) **`citations` (3918 rows) = bibliography references** extracted from papers (paper A cites paper B) — and **0 of them are resolved to an in-corpus document** (`target_document_id IS NOT NULL` → **0**, verified). That's the **citation-graph** feature, and it is *not* what this row is about. (2) **Answer citations = the inline `[n]` markers** mapping a claim to the retrieved chunk that supports it — **that** is this row, and its data is `answer_claims` **168** / `answer_records` **26**, plus `result.sources` per turn (already rendered by `SourceCard`/`SourcePanel`). **So: still a presentation/linking feature, but on the answer path, not the bibliography.** **Read `synthesis.py` first — the `[n]` contract is load-bearing:** `_CITATION_RE = \[(\d+)\]` (`synthesis.py:23-25`) parses markers *"produced by ANSWER_PROMPT"*, and `:30-38` records a **2026-07-14 fix where haiku emitted `[Source 2]` / `[2, 4]` and the parser dropped them — so claims that DID cite read as uncited**. Any highlighting UI inherits that parser's blind spots. **Naming trap:** `CitationAudit.clean` (`:173-175`) is `not out_of_range and not malformed`, so an answer with **zero** citations is `clean == True` — the real signal is `n_uncited_sentences` (`:170`); don't build a green badge on `clean`. **Open:** highlight *in the extracted markdown* (cheap, data exists) vs *in the original PDF* (that's the parked in-app PDF-viewer row — much bigger). **Decide that first; they're different features.** |
| [ ] | **Improve the chunk screen (Library browser)** — a better chunk-reading surface: navigation, context, provenance, and per-chunk state | new (2026-07-17, user request) · overlaps the existing "Chunk editing + color-coded chunk state" row (which **needs an ADR** — editing collides with the Enrichment-Layer non-negotiable) | **30 882 chunks live** (the `chroma_pc` index the API loads). **Split the read half from the write half:** improving *reading* (navigation, context, provenance display) is plain UI over shipped data and needs no ADR; *editing* chunks is the ADR-needing row above. **Don't bundle them.** Note the two Chroma dirs (`data/chroma` 11 965 vs `data/chroma_pc` 30 882) — confirm which the UI reads before building anything that counts chunks. |
| [ ] | **Improve the ingestion workflow** — a better end-to-end ingest experience (progress, failures, retry, what-changed, per-file outcomes) | new (2026-07-17, user request) · builds on the shipped S1/S2 selective-ingestion work | **Data exists: `source_files` 77 · `ingestion_events` 76 · `documents` 76** (measured 2026-07-17); the registry already derives status (`new`/`changed`/`ingested`/`missing`, "computed at read time, persisted nowhere") and `Sources.svelte` already renders status chips + the `excluded` toggle. So the **model and most of the read surface exist** — this is about the *flow*, not new data. **Interacts with two planned PRs:** library-only delete writes `SourceFile.excluded` (plan PR-5), and the missing-source badge derives from `resolve_source_path` (plan PR-3/PR-4) — **sequence after those or re-grep**. Needs `grill-me` to scope (what's actually painful today?). |
| [ ] | **Improve the settings screen** — reorganize/clarify Settings; today it mixes cosmetic prefs, per-install choices, and the RAG sandbox in one modal | new (2026-07-17, user request) | Mostly presentation, **but it must preserve ADR-010's persistence boundary**: cosmetic prefs (theme) and per-install choices (source dir, provider) persist via `app_settings`/`localStorage`; **retrieval-quality knobs are non-persistent by decision** and live in the in-memory `overrides` (`App.svelte:87`, "a fresh launch always starts from `{}`"). A redesign that makes the sandbox *look* like settings would invite exactly the persistence the ADR forbids. **Related:** plan PR-1 adds a muted "(off in evidence-only mode)" hint to the multi-query row (`Settings.svelte:405-410`). **Distinct from the RAG-tuning row below** — that one changes governance; this one is layout/clarity only. |
| [ ] | **User-tunable RAG pipeline** — let the user change the retrieval/synthesis pipeline (weights, k's, rerank, multi-query, chunking) from the app | new (2026-07-17, user request) — **⚠ NOT a UI PR: needs `grill-me` + an ADR (reopens ADR-010) BEFORE any build** | **This collides head-on with a written decision.** ADR-010 **considered and REJECTED option 2 (persistent editable settings) explicitly "on governance grounds"** — it would violate the locked-settings non-negotiable; **non-persistence IS the governance wall** ("overrides never written to `config`/`.env`/`app_settings`; restart returns to the eval-gated baseline"). ADR-010 also fixes the **split**: query-time knobs (`TOP_K`, `SYNTHESIS_MODE`, `USE_MULTI_QUERY`, + the 2026-07-10 amendment's `EPISTEMICS_MARKERS_ENABLED`/`REVIEWER_EVIDENCE_CHARS`) are live + non-persistent; **construction-time** (`CANDIDATE_K`, retrieval weights, reranker, provider/model) and **ingest-time** (chunk sizes, `USE_PARENT_CHILD`) are **read-only**. And CONTEXT's non-negotiable: a locked setting changes **only** via an eval-harness experiment (`--repeat`, beat the control, record a baseline) — *"a sandbox **override** is fine; changing a **default** is not a UI PR."* **Grill branches:** which knobs become tunable (query-time only, or the read-only tiers too)? does a change persist, and if so what stops it silently invalidating every eval baseline? does the eval harness gate a user's change, and who runs it? is this "a better sandbox" (no ADR — ADR-010 already allows per-session overrides) or "user-owned defaults" (**reopens ADR-010**)? **The A/B "Test override" compare card already exists** and is the natural host for the honest version. **⚠ MEASURED 2026-07-17 — the user's pushback ("I don't see the problem with changing top-k; the issue is the embedding") is CORRECT, and it exposed a real gap in the governance model.** (1) **`TOP_K` is ALREADY exactly what's wanted** — per-session, non-persistent, **and bounded**: `top_k: int | None = Field(default=None, ge=1, le=CANDIDATE_K)` (`apps/api/models.py:58`) → range **[1,20]**, **422 on out-of-range, never a silent clamp**; `Settings.svelte:381` → `ChatRequest.overrides` → `chat_controller.py:758`. There was never a problem here. (`REVIEWER_EVIDENCE_CHARS` is likewise bounded [200,6000], `models.py:62`.) (2) **The embedding IS the only catastrophic knob — but not by the assumed mechanism.** Collections are **namespaced per model** (`embeddings.py:104-114` `get_collection_name()`: `bge-base`→`"langchain"`, others→their own key), so pointing at another model reads an **EMPTY collection, not garbage** → `pipeline.py:148-154` logs `warning("empty_index")`, falls back to vector-only, and answers "the sources don't contain the answer"; `embeddings.py:98-101` raises `ValueError` on an unregistered key, so an arbitrary HF id can't be typed. **The real footgun is narrower:** `_LEGACY_COLLECTION = "langchain"` (`:78-82`,`:111-114`) is bound to **whatever `DEFAULT_MODEL` is**, and **both registered models are 768-dim** (`:66,73`) — so if `DEFAULT_MODEL` ever changed, the new default **silently inherits bge-base's vectors**, Chroma accepts the dimension, and you get exactly the catastrophic incomparability. **Nothing detects it** — the guard is namespacing alone; there is **no version stamp and no mismatch check** (`get_active_model_name()` is recorded per answer/similarity edge, so you can audit *after the fact*, never at query time). (3) **THE GAP: `EMBEDDING_MODEL` appears in NEITHER ADR-010's three-way split NOR CONTEXT's locked-settings table.** `.claude/CONTEXT.md:77` lists it in the *stack* table as "swappable via `EMBEDDING_MODEL`". **The most dangerous knob in the system is documented as swappable and governed by nothing, while `TOP_K` — harmless and already user-facing — is locked.** **Why: ADR-010 sorts by *cost to change* ("the exposed set equals the cheap set equals the honest set", `:128`); the user is sorting by *blast radius*. Those axes disagree, and blast-radius is unmapped anywhere.** The grill should adopt it: **(a) this query only** — `TOP_K`/`SYNTHESIS_MODE`/`USE_MULTI_QUERY`/`EPISTEMICS_MARKERS_ENABLED`/`REVIEWER_EVIDENCE_CHARS` (all already request-scoped); **(b) reversible, no re-ingest** — `CANDIDATE_K`, `BM25_WEIGHT` (measured **inert post-rerank**, `config.py:210-214`), reranker, provider/model (`set_chat_model` swaps with **no rebuild**, `pipeline.py:171-184`); **(c) needs re-ingest, contained** — chunk sizes/overlaps, `USE_PARENT_CHILD` (also switches the Chroma **directory**, `config.py:83-84`), `PDF_EXTRACTOR`; **(d) catastrophic** — `EMBEDDING_MODEL`, only via the `DEFAULT_MODEL` path. |
| [ ] | **Per-document ingestion tuning** — let the user choose chunking (and maybe extractor) per document, so a table-heavy or unusual paper can be ingested differently from prose | new (2026-07-17, user request: *"I'd like the user to be able to fine-tune the ingestion in some cases"*) — **needs a spec; grill with the RAG-tuning row above** | **⚠ MEASURED 2026-07-17 — the intuition is RIGHT (the vector space survives) but it is contained, NOT free.** **Survives:** `build_parent_child_chunks` (`chunking.py:160-178`) embeds every child with the **same model** → same 768-d space → cosine stays comparable regardless of chunk length; the reranker scores `(query, chunk)` pairs **independently** (`pipeline.py:227-233`, zero cross-doc coupling); parent-child projection is **per-document** (`:238-257`); `sweep_chunking.py`'s own header confirms the only cost is re-embedding — *"a chunk-size change invalidates the embedding **cache**"* (the cache, not the space). Epistemics' `{document_id}:{chunk_index}` key is positional, so re-chunking one doc orphans **only that doc's** sidecar rows — which *supports* containment. **But FOUR things assume uniformity, and none are what ADR-010 names:** (1) **BM25's `avgdl` is corpus-GLOBAL** — `BM25Retriever.from_documents(all_docs, …)` (`pipeline.py:144`) over `rank_bm25` length-normalizes every chunk against a **corpus-wide mean**, so mixing 400-char and 1500-char children **systematically penalizes the longer-chunked document on the sparse arm**. *This is the one genuine cross-document coupling, and it's invisible today only because uniformity hides it.* (2) **Health thresholds are absolute** (`health.py:69-86`: `chunks_per_page < 2 or > 15`, `avg_chunk_length < 300`) and calibrated to `BASELINE_CHUNK_SIZE=1000` → a doc correctly chunked at 3000 is **flagged unhealthy for being correct**. (3) **`TOP_K` counts PARENTS, not tokens** (`:238-257`) → context volume = `top_k × PARENT_CHUNK_SIZE`, so a per-doc `PARENT_CHUNK_SIZE=8000` **quadruples context and cost on every turn that retrieves it — no guard anywhere**. (4) Doc vectors **mean-pool over chunks** → granularity shifts the doc vector → moves `WIKI_MIN_SIMILARITY=0.90` clustering + `similar_docs`. **Not representable today, and there's a trap:** `Document` (`db/models.py:141-168`) stores `extractor_used`/`extraction_health`/`chunk_count`/`page_count` but **no chunk sizes and no embedding_model** (the pattern exists elsewhere — `DocSimilarity.embedding_model:295`, `AnswerRecord.embedding_model:634` — just not on `Document`); and **the splitters are import-time singletons** (`chunking.py:62-63`), used directly by `build_parent_child_chunks` (`:167,170`), so **mutating `config.PARENT_CHUNK_SIZE` at runtime does NOTHING on the hot path** — the factories read config at call time only for test monkeypatching, which is why `sweep_chunking.py` shells out via `subprocess` + `ingest --rebuild` per config. **Work = thread a chunk-config object through `build_parent_child_chunks` (retiring the singletons) + columns on `Document` + a migration + make health thresholds relative to the doc's own config + decide the BM25 `avgdl` question.** |
| [ ] | **User-customizable chat modes (swappable system prompts)** — named modes ("explain to a novice", "be terse", "focus on methodology") that swap parts of the system prompt | new (2026-07-17, user idea) — **needs a spec; the boundary below is non-negotiable** | **⚠ MEASURED 2026-07-17 — the idea is good, with ONE hard boundary: `ANSWER_PROMPT` is not a prompt, it is the WIRE FORMAT the integrity layer parses.** `synthesis.py:23-25` literally comments that `_CITATION_RE = \[(\d+)\]` matches markers *"produced by ANSWER_PROMPT"*, and `prompts.py:48-55`'s bullet list (never `[Source 3]`, never `[2, 4]`, never words in brackets) is a **direct mirror of the parser's grammar**. **There is empirical proof the coupling already broke:** `synthesis.py:30-38` records a **2026-07-14 fix** where haiku emitted `[Source 2]` / `[2, 4]` / `[2 and 4]`, the bare-`[n]` parser dropped them, **and claims that DID cite read as UNCITED** — the model merely *drifting* from the prompt corrupted the integrity layer. A user editing it is that failure with the safety off. **Concrete silent failure chain if the citing instruction is removed:** `cited_source_numbers` (`:42-50`) → `[]` → `claim_marker` (`:106-118`) `has_real=False` → **every** claim `MARKER_UNSUPPORTED` → every line badged "⚠ unsupported" (`:252-258`) → `record_claims` (`provenance.py:211-227`) **persists false `unsupported` rows into the adjudication log** → the `MIN_FAILURE_TAG_COUNT`/`_DOCS` gates (`config.py:331-335`) count them → **the self-improvement loop learns from corrupted data.** No exception, no warning, indistinguishable from "the model is hallucinating". (The reviewer survives — it asks for JSON and grades semantically — but would fire `missing_citation` as a **false alarm blamed on the model**.) **⚠ Naming trap:** `CitationAudit.clean` (`:173-175`) = `not out_of_range and not malformed`, so an answer with **ZERO** citations is `clean == True`; the real signal is `n_uncited_sentences` (`:170`) — don't build a green badge on `clean`. **⚠ One line any prompt-swap MUST fix:** `chat_controller.py:516` computes `self._answer_template_hash = template_hash(str(ANSWER_PROMPT))` **once at construction** — `prompt_version_hash` already folds it into provenance, so versioning is nearly free, but that cached hash **goes stale the moment prompts are runtime-swappable and every provenance record would lie about which prompt produced it**. Move it per-turn. **THE CARVE: the citing block of `ANSWER_PROMPT` is NOT user-editable; the persona/task framing above it IS.** That yields real modes while the `[n]` contract — and everything downstream — holds. **Cheap to swap safely:** `MULTI_QUERY_PROMPT`, `REWRITE_PROMPT` (`prompts.py:5,27`), `_SUMMARY_PROMPT` (`wiki.py:427`), and the enrichment prompts. **No registry exists** — prompts are `_`-prefixed module constants beside their consumers; **no prompt appears in `RagOverrides`/`config`/`app_settings`**. But the mechanism is pre-shaped: chains bind `chain = PROMPT | self.llm` **fresh every call** (`pipeline.py:171-184`), so a swap takes effect per-call with no rebuild. `synthesis_mode` is **effectively the first chat mode but structurally not one** — it skips the interpretation call entirely (`chat_controller.py:823-838`), i.e. a *pipeline-topology* mode, not a prompt swap; a registry generalizes it only with a null entry. `result.mode` is already on the wire with **zero consumers** — a free field for a mode system. **The deliberate exception is the parked "Unconstrained mode" row above** (corpus restraint off, measurements on) — its own note already says it needs a grill, precisely because keeping the measurements honest while turning restraint off is the hard part. **⚠ Dead-code trap:** `core.py:53,67` has a stale `REWRITE_PROMPT`/`ANSWER_PROMPT` — deleted in `b3ffa06` but **gitignored and still on disk**, so it's grep-visible and entirely dead. **Don't edit it.** |

| [ ] | **BUG — the claim segmenter counts markdown headings as claims** — every heading in an answer becomes an `answer_claims` row, is structurally uncitable, and is therefore permanently marked `unsupported` | found 2026-07-17 while validating the gap payload (RG-014) — **NOT UI: `src/doc_assistant/synthesis.py` claim segmentation** | **Measured on the live corpus: of the 61 `unsupported` claims, 12 are markdown HEADINGS** (e.g. `"# Dense Passage Retrieval (DPR) and Its Advantages Over BM25"`) **+ 8 are short fragments = 20/61 (33%) are not prose claims at all.** A heading can never carry a `[n]` citation, so it is *structurally* always unsupported — this is a **permanent false positive**, not model drift. **Blast radius is wider than the badge:** these rows (1) inflate `unsourced_claim` gaps (ADR-004 — 4 of the 14 live gaps are built on this input), (2) get persisted to the adjudication log by `record_claims`, and (3) **feed the `MIN_FAILURE_TAG_COUNT`/`_DOCS` gates (`config.py:331-335`) that drive the self-improvement loop — so the loop is learning from noise.** Fix = filter headings/fragments before segmenting (or mark them `not-a-claim` rather than `unsupported`). **Note the related known drift bug** (`synthesis.py:30-38`, fixed 2026-07-14): the model emitted `[Source 2]`/`[2, 4]` and the parser dropped them, so claims that DID cite read as uncited — **43 of the 61 unsupported claims predate that fix**, so the historical adjudication log is doubly contaminated. Consider whether the pre-2026-07-14 claims should be re-scored or excluded from the gates. |

| [x] | **Corpus groups = Folders with retrieval scope** — folder CRUD + Library filter + per-turn "chat with this folder" scope (an unresolvable scope searches nothing, never everything) + demo auto-folder | new (2026-07-20) | **COMPLETE: F1 `3969adb` · F2 `0e45dd3` · F3 `217a122` (2026-07-20).** ADR-025; specs `feature-corpus-folders{,-scope,-demo}.md`. Bounds owed: RG-020 ($in @10k) · RG-021 (eval fingerprint). |

<!-- Add new UI ideas below this line as `| [ ] | <element> | <where> | <notes> |` -->

> **§3 note — the 2026-07-17 plan file (LOCAL, outside the repo).** Evidence-only · missing-source +
> library-only delete · extended metadata were explored and planned in depth on 2026-07-17 and carved into
> **8 PRs** (PR-1 evidence-only · PR-2 DOI · PR-3 `resolve_source_path` fix · PR-4 badge · PR-5 library-only
> delete · PR-6 ADR-016 · PR-7 fields+editor · PR-8 Crossref runner), sequenced **after** tag-families
> PR-2.5/2.6/2.7. Per-PR files, seams to reuse, decisions, tests, verification and risks live in
> `C:\Users\LDELEZ\.claude\plans\perfect-let-s-plan-some-tidy-tulip.md` — **Claude Code's local scratch, NOT
> committed**. The load-bearing measurements and every corrected claim are duplicated into the three rows
> above and into `docs/DEVLOG.md` (2026-07-17) so nothing is lost if that file disappears. **When each PR is
> picked up, write its `docs/specs/` contract from the plan first** (the repo's spec-at-build-time pattern).

---

## 4 · Per-feature review checklist (run before marking any UI feature "done")

The gate for every new UI element. Derived from this repo's non-negotiables (`.claude/CONTEXT.md`
§Non-negotiable rules), ADR-010/011, and the DoD blocks in `docs/specs/feature-phase8-ui-upgrade.md`.
Copy this block per feature; check every applicable line before moving it to *Shipped*.

### Architecture & conventions
- [ ] **Thin shell** — no business logic in `apps/`; all logic in `src/doc_assistant/`. UI → library, never the reverse. (CONTEXT rule 3.)
- [ ] **Request-scoped, no module-global mutation** — any per-turn/session knob is threaded as an explicit parameter (a frozen dataclass / wire model), never assigned to a `config.*` or other module global. Concurrent turns on the shared `ChatController` singleton cannot leak into each other. (ADR-010; there's an isolation-guard test — extend it to any new field.)
- [ ] **Effective-value truthfulness** — anything provider/model-dependent reads the *effective* value (`self.rag.provider`/`.model`, `app_settings.effective_llm()`), never the import-time `LLM_PROVIDER`/`LLM_MODEL` constants (they go stale after a live switch). (ADR-011.)
- [ ] **No locked-setting change** without an eval-harness experiment + baseline (`--repeat`, beat control). A sandbox *override* is fine; changing a *default* is not a UI PR. (CONTEXT §Locked settings.)
- [ ] **Persistence boundary** — retrieval-quality-governed knobs are non-persistent (ADR-010). Only cosmetic prefs (theme) or per-install choices (source dir, provider selection) persist, and those go through `app_settings`/`localStorage`, never `.env`/`config.py`.
- [ ] **Exceptions chain** (`raise X from e`); user-facing messages translated at the UI/API boundary; a sidecar failure (markers, provenance) never breaks the turn.
- [ ] **Secrets** — no key in code or client; keys stay in `.env`. (CONTEXT rule 2.)

### Correctness & data integrity
- [ ] Default path (feature off / `overrides=None`) is **byte-identical** to before the feature.
- [ ] Bounds enforced at the validation boundary (pydantic wire model): out-of-range → 422, never a silent clamp. Note if a non-HTTP caller could bypass them.
- [ ] Any value persisted or recorded is **honest** — labels match the actual model/provider/kind that ran (see the `reviewer_kind` finding).
- [ ] Empty/whitespace/malformed input rejected server-side, not only guarded in the client.

### Frontend
- [ ] `svelte-check` — 0 errors.
- [ ] **Both themes** — light *and* dark styled; a new color reuses existing CSS vars (no new palette). Manual theme still wins over the OS media query.
- [ ] **Responsive** — no horizontal body overflow at mobile width; wide content scrolls in its own container.
- [ ] **Accessibility** — interactive controls have roles/labels (`role="radiogroup"`/`radio`, `aria-checked`, `aria-live` for async status); reduced-motion respected for transitions.
- [ ] TypeScript wire types mirror the backend model (`types.ts` ↔ `apps/api/models.py`).

### Tests & verification
- [ ] Unit + integration tests for the new behavior (incl. the no-global-mutation / isolation case where applicable); full gate green (`ruff` / `ruff format` / `mypy --strict src` / `bandit` 0 HIGH·MED / `pytest`).
- [ ] **Preview-harness verified** — at minimum construction-only ($0). For anything touching a real turn, a **live-UI smoke test** on a real answer (route through local Ollama to keep it $0 — force `--provider ollama`, then restore `.env`; watch the Anthropic credit-leak, KI/CONTEXT §Provider config).
- [ ] No live paid API calls in tests (cpc §13, gate-enforced).

### Docs
- [ ] One `docs/DEVLOG.md` entry (What / Why / Rejected / Opens).
- [ ] ROADMAP U-row updated (built → committed `<sha>`).
- [ ] This checklist updated: box checked in §1, item removed from §2/§3.
- [ ] A non-obvious design choice → an ADR in `docs/decisions/`; a stress-tested spec → `docs/specs/` (design-locked via a grill pass).
